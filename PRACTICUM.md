# Практикумы

Архив и классификация отвечают на вопрос «какие задачи бывают». Практикумы отвечают
на следующий: «как пройти через все нужные приёмы и довести их до автоматизма».
Формат перенесён из [ioai-prep](https://github.com/IceDarold/ioai-prep) — ноутбук,
в котором теория сразу переходит в лестницу заданий от простого к сложному,
с самопроверкой и финальным заданием на таймере.

## Что переносится из ioai-prep, а что нет

Работающая часть формата там держится на четырёх решениях.

**Лестница внутри одной темы.** Практикум не про «свёрточные сети вообще», а про
конкретный узкий навык, разложенный на 6–8 ступеней: 🟢 приём в чистом виде,
🟡 приём в неочевидной обёртке, 🔴 комбинация приёмов. Последнее задание почти всегда
требует обобщить то, что до этого делалось тремя разными способами.

**Самопроверка без подглядывания.** Каждое задание заканчивается ячейкой, которая
говорит «сошлось» или «не сошлось», не показывая ответа. Это позволяет упереться
и попробовать снова, а не сверяться с ключом после первой же трудности.

**Задание на таймере с журналом попыток.** В конце ноутбука — задача, которую
надо решить по памяти с закрытым верхом, с записью времени. В praktikum-01
журнал выглядит так: `12.07 — 7 минут`, `23.07 — 2:58`. Возврат к одной и той же
задаче через неделю и через месяц — то, что превращает понимание в скорость.

**Решения в конце, а не рядом.** Отдельный раздел `🔑 Решения`, физически
далеко от заданий.

Не переносится одно: там задание — «напиши код», и `assert` проверяет написанное.
В математике решение живёт на бумаге, и ячейка не может его увидеть. Поэтому
механика проверки заменена целиком, см. раздел «Самопроверка».

## Четыре уровня

| Уровень | Что это | Сколько | Где живёт |
| --- | --- | --- | --- |
| Раздел | пять разделов программы AA HL | 5 | навигация, ноутбука нет |
| **Практикум** | **один ноутбук** | **36** | `practicum/<раздел>/*.ipynb` |
| Приём | блок внутри ноутбука | 5–9 на практикум | `practicum/skills/*.yaml` |
| Упражнение | одно задание | 8–12 на практикум | ячейки ноутбука |

### Правило гранулярности

Практикум — это **60–200 баллов** исходного материала архива, **5–9 приёмов**,
**одна связная лестница**. Границы не условные: меньше 60 баллов — на теме нельзя
набрать достаточно практики, и её надо слить с соседней; больше 200 — ноутбук
не проходится за один заход, и тему надо резать.

Связность важнее объёма. «Алгебра» целиком — не практикум, но и «всё, что помечено
темой `functions.equations`» — тоже не практикум, потому что внутри лежат
несовместимые вещи. Признак верной нарезки: приёмы выстраиваются в лестницу,
где следующий **сводится к предыдущему** или расширяет его. Если приёмы
просто соседствуют по названию темы, разрез сделан не там.

Карта лежит в [`practicum/map.yaml`](practicum/map.yaml), а правило проверяется:

```bash
python practicum/check_map.py
```

Скрипт считает баллы по корпусу, показывает практикумы вне диапазона, темы, не
попавшие ни в один практикум, и темы, доли которых не складываются в единицу.
Осознанный выход за границу оформляется полем `exception` с обоснованием —
скрипт такие случаи показывает, но ошибкой не считает. Ровно один такой случай
есть сейчас: E7, см. ниже.

### Приём — то, чего в классификации пока нет

В корпусе у каждого блока есть `method_tags` и `method_path`. Это ценный материал,
но как основа для ноутбуков он не годится напрямую: **2086 различных тегов на 4745
употреблений, из них 1392 (67%) встречаются ровно один раз**. Рядом живут
`substitution`, `substitute` и `substitute_value`; `solve_quadratic`,
`quadratic_formula`, `quadratic_solving` и `quadratic_equation`. Это свободный
текст, а не словарь.

Поэтому между архивом и ноутбуком вводится нормализованный слой — карточка приёма
в `practicum/skills/<тема>.yaml`. Образец: [`calculus-differential-equations.yaml`](practicum/skills/calculus-differential-equations.yaml).

```yaml
- id: integrating_factor
  name: Интегрирующий множитель
  rung: 5                    # место на лестнице
  trigger: >                 # по какому признаку приём узнаётся в условии
    Уравнение линейно по y: приводится к dy/dx + P(x)·y = Q(x).
  chain: [...]               # логическая цепочка шагов
  traps: [...]               # где теряются баллы, по markscheme
  calculator:                # что меняется, когда калькулятор разрешён
    mode: checks
    note: >
      Ни один GDC не строит интегрирующий множитель...
  prereq: [separation]       # какие приёмы нужны раньше
  raw_tags: [...]            # какие сырые теги корпуса схлопнуты сюда
  blocks: [...]              # вопросы архива, на которых приём отрабатывается
```

Два поля здесь несут основную нагрузку.

`raw_tags` — это мост. Он позволяет по любому вопросу архива узнать, в каком
практикуме он отрабатывается, и наоборот, и он же собирает синонимы в один приём.

`trigger` — единственное поле, которого в архиве нет вообще и которое нельзя
извлечь автоматически. Классификация фиксирует, **как** задача решается;
триггер фиксирует, **по какому признаку это видно до начала решения**. На экзамене
дифференциальное уравнение решается не тем, что вы помните формулу интегрирующего
множителя, а тем, что за несколько секунд опознали линейное уравнение.
Это и есть то, ради чего практикумы затеваются.

### Paper 1 и Paper 2: где калькулятор меняет дело

**60% баллов архива идут с калькулятором** (Paper 2 — 40%, Paper 3 — 20%). Но доля
эта по темам скачет от **16% у тригонометрических уравнений до 100%**, поэтому
единый раздел про GDC в каждом ноутбуке был бы в одном месте мёртвым грузом,
в другом — недостаточным. Доля считается по корпусу и выводится колонкой `GDC`
в `check_map.py`.

Два числа, которые стоит знать, прежде чем полагаться на калькулятор:

- в **85% вопросов Paper 2 и 3 разметка markscheme вообще не упоминает GDC**.
  Оговорка: markscheme фиксирует метод, за который дают баллы, и часто показывает
  алгебраический ход, принимая при этом ответ с калькулятора. Так что 85% — это
  верхняя оценка «калькулятор не нужен», а не факт;
- **20% вопросов с калькулятором требуют точную форму, «show that» или
  доказательство**. Там десятичный ответ баллов не приносит вовсе.

Поэтому калькулятор описывается **на уровне приёма, а не практикума** — полем
`calculator` в карточке. Слов накопилось тринадцать, от «нужен» до «бесполезен»:

| `mode` | Что означает |
| --- | --- |
| `required`, `needed` | приём существует только с калькулятором |
| `replaces` | с калькулятором приём не нужен, задача решается напрямую |
| `speeds_up` | метод тот же, калькулятор снимает арифметику |
| `helps` | помогает не везде: различает бумага, а не задача |
| `partial` | часть хода берёт на себя, часть остаётся руками |
| `checks` | в решении не участвует, годится для проверки ответа |
| `yes` | вопросы приёма стоят в бумагах с калькулятором |
| `allowed` | формально разрешён, но считать нечего |
| `mixed` | где как: часть блоков Paper 1, часть с калькулятором |
| `forbidden`, `no` | требуется точная форма, десятичный ответ баллов не даст |
| `none` | бесполезен совсем, включая блоки из бумаг с калькулятором |

Это те же слова, что печатает шапка карточки на странице повторения, и словарь
перевода там держит `check_skills.py`: тринадцать слов против шести в словаре
давали в шапке «калькулятор allowed» английским посреди русской строки. Заодно
проверка ловит `mode: no` без кавычек — YAML делает из такого булево, и до
страницы доезжает не слово, а `false`.

Значение выбирается по данным, а не на глаз: для блоков приёма считается доля
вопросов с калькулятором и доля требующих точную форму. У `homogeneous_substitution`
это пять «show that» из семи — отсюда `forbidden`; у `euler_method` семь Paper 2
из семи — отсюда `required`.

В ноутбуке под это отводится **один блок «то же самое в Paper 2»**, а не раздел
про кнопки: берётся уже решённый вопрос из Paper 1 и перерешивается с калькулятором,
чтобы два маршрута стояли рядом. В C3 это `2cos²x + 5sin x = 4` — на бумаге семь
баллов работы, с графиком десять секунд. В E7 наоборот: калькулятор не даёт
замкнутой формы, зато проверяет готовый ответ методом Эйлера с мелким шагом.

Четыре правила экзаменационного формата, которые к технике решения отношения
не имеют, но стоят баллов: ответ в Paper 2 по умолчанию до трёх значащих цифр;
«hence» обязывает опереться на предыдущий пункт, то есть решать заново
калькулятором нельзя, а «hence or otherwise» — можно; «exact» и «show that»
запрещают десятичный ответ; режим градусов и радианов.

## Что где лежит

```
practicum/
  map.yaml            карта 36 практикумов, источник правды
  check_map.py        проверка правила гранулярности и покрытия тем
  kit.py              проверочный набор, общий для всех ноутбуков
  make_kaggle.py      сборка и публикация Kaggle-версий
  skills/             карточки приёмов по темам
  generators/         скрипты, собирающие .ipynb
  tests/              прогон эталонных ответов и типовых ошибок
  calculus/ geometry/ … сами ноутбуки
```

**Ноутбуки собираются скриптом, а не правятся руками.** Файл на 50 ячеек JSON
редактировать вручную нельзя, поэтому исходником считается генератор в
`generators/`, а `.ipynb` — производное. Правка идёт так: изменить генератор,
пересобрать, прогнать тесты, опубликовать.

Тесты в `tests/` проверяют три вещи и запускаются по одному файлу на практикум:
все эталонные ответы дают `✅`, типовые ошибки дают `❌`, а незаполненный ответ
даёт `⬜` вместо падения. Отдельно `test_kit_roots.py` проверяет саму механику
`verify_roots` — что она ловит потерянные и посторонние корни.

## Анатомия ноутбука

```
Титул: цель, объём материала из архива, порядок работы, метки сложности
Ячейка настройки: from kit import *
Карта приёмов: таблица «приём — триггер — к чему сводится»
Теория 1  →  Задание 🟢  →  Задание 🟢
Теория 2  →  Задание 🟡  →  Задание 🟡
Теория 3  →  Задание 🔴
Тренажёр распознавания: 10–12 условий, назвать приём, без вычислений
Задание на таймере + журнал попыток
🔑 Решения — цепочками шагов, со ссылкой на источник
```

Три правила наполнения.

**Каждое задание — настоящий вопрос архива, с указанием источника.** Придуманные
задачи не годятся: смысл проекта в том, чтобы пройти реальный спектр формулировок.
Провенанс сохраняется так же, как в Question Atlas.

**Теория пишется под ловушки, а не под учебник.** Изложение отвечает на вопрос
«почему именно так» и заранее называет места, где теряются баллы: минус при
интегрировании `y^-2`, потерянное слагаемое `v` в производной произведения,
округление до десятичных знаков вместо значащих цифр. Всё это берётся из
`traps` карточек приёмов, а те — из markscheme.

**Тренажёр распознавания обязателен.** Блок, где по десятку условий надо назвать
приём, ничего не вычисляя. Это самая дешёвая и самая недооценённая часть
подготовки: она тренирует ровно тот навык, который отделяет решённый вопрос
от нерешённого при ограниченном времени.

## Самопроверка

Ответ проверяется **по существу задачи, а не сравнением с записанным эталоном**.
Набор функций — [`practicum/kit.py`](practicum/kit.py).

### Как записывают ответ

`from kit import *` приносит не только проверки, но и математические имена:
`sin`, `cos`, `tan`, `cot`, `sec`, `cosec`, `arcsin`, `arccos`, `arctan`, `sqrt`,
`exp`, `log`, `ln`, `pi`, `E`, `I`, `oo`, `Rational`, `solve`, `diff`, `integrate`,
`Eq`, `simplify` и ещё десяток. Ответ пишут так, как он выглядит на бумаге:

```python
my_roots = [pi/6, 5*pi/6]          # а не [sp.pi/6, 5*sp.pi/6]
my_cot   = -sqrt(5)/2
my_f     = 4*cos(x)/sin(x) + sin(x)
```

Смысл не в экономии трёх букв. Практикум тренирует математику, и всё, что
отвлекает на синтаксис библиотеки, работает против него; к тому же `sp.` в ответе
не даёт разглядеть в нём формулу. IB-написание тоже принимается: `arccos`
и `cosec` — псевдонимы к `acos` и `csc`.

Список импортируемых имён в `kit.py` **явный**, а не `from sympy import *`:
звёздочка тянет под тысячу имён и молча перекрывает часть встроенных, в том
числе `N` и `S`, а ловить такое в чужом ноутбуке дороже, чем выписать список.
`import sympy as sp` в ячейке настройки остаётся запасным выходом ко всему
остальному.

Единообразие стережёт [`practicum/tests/check_notation.py`](practicum/tests/check_notation.py):
он проходит по всем ноутбукам и падает, если `sp.` или `P` вернулись в код —
в том числе в блоки ```python внутри решений, откуда списывают чаще всего.

Карточки приёмов стережёт [`practicum/tests/check_skills.py`](practicum/tests/check_skills.py):
разбор YAML, совпадение `ladder` со списком приёмов, заполненность `trigger`,
существование файлов по ссылкам из `map.yaml` и — главное — что все номера
блоков в карточках действительно есть в корпусе. Поводом стали три случая,
когда карточка вообще не разбиралась: двоеточие внутри фразы («два разных
случая: при одном…») превращает строку в отображение. Пока карточки читал
только человек, ошибка жила незамеченной.

`verify_ode(label, y_expr, rhs, ic)` подставляет ответ в исходное уравнение и
считает невязку. Эталона в ячейке нет вообще, подсмотреть нечего, и при этом
любая верная форма записи проходит.

`verify_implicit(label, got, want)` принимает неявный ответ `F(x, y) = c` в любой
форме, отличающейся постоянным множителем: `x² − 2xy − y² = 4` и
`y² + 2xy − x² + 4 = 0` засчитываются одинаково — как в markscheme.

`check_num(label, value, sf, digest)` сверяет число по хешу с округлением до
заданного числа **значащих цифр**, потому что именно в этой формулировке
вопросы и ставятся.

`verify_roots(label, roots, expr, domain)` — для уравнений, где ответ это множество
корней. Каждый корень подставляется в уравнение, а **полнота набора проверяется
независимым численным сканированием отрезка**. Это ловит главную ошибку
тригонометрии: корни верны, но найдены не все. Сообщение называет, около какой
точки пропажа, не выдавая самого корня. Флаг `deg=True` — для вопросов в градусах.

`verify_inverse(label, got, f, domain)` — для ответа-функции: эталона нет,
внутрь вашего ответа подставляется сама `f`, и требуется `x` обратно на области
из условия. Направление именно такое: `f(g(x)) = x` пропускает неверную ветвь
корня, `g(f(t)) = t` её ловит, а выбор ветви и есть R1 экзамена. Рядом
`check_domain(label, got, digest)` сверяет область или множество значений как
множество: запись свободна, концы — нет.

`verify_transform(label, got, source, target)` — для ответа-рецепта:
последовательность преобразований записывается списком шагов, применяется
к исходной функции по очереди, и результат сверяется с целевой. Эталонного
описания нет вовсе, поэтому любой работающий порядок принимается, а
неработающий отвергается — с указанием, что именно не так: порядок,
обратная величина растяжения или сторона сдвига.

`verify_sketch(label, got, f, domain)` — для ответа-эскиза: он записывается
словарём особенностей (`x_intercepts`, `y_intercept`, `maxima`, `minima`,
`cusps`, `vertical_asymptotes`, `horizontal_asymptotes`, `oblique_asymptotes`,
`endpoints`), и каждая из них считается из самой функции. Пропущенное
и лишнее — разные сообщения, потому что в markscheme это разные баллы;
координаты сверяются с точностью до трёх значащих цифр, как их принимает
экзамен. Точки поворота ищутся численно: тема живёт на модулях, у `|f|`
производная содержит `sign(...)`, и `solveset` с ней не справляется.

`verify_asymptotes(label, got, f, kinds)` — для ответа-прямой: асимптота
записывается уравнением (`Eq(x, 3)`, `Eq(y, x/2 - Rational(17, 2))`, годится
и `Eq(2*x + 6, 0)`), и каждая проверяется пределом по определению —
односторонний предел бесконечен, предел на бесконечности равен числу, разность
`f(x) − (ax + b)` стремится к нулю. Голое число не принимается: markscheme
пишет *«must be written as an equation with y =»*. `kinds` сужает вопрос до
того вида, о котором спрашивают. Пропущенная асимптота и лишняя — разные
сообщения.

`verify_range(label, got, f, domain)` — для ответа-множества значений: проверка
ничего не решает за вас, она спрашивает у самой `f`, достигается ли значение.
Внутри вашего множества — должно, снаружи — не должно, и отдельно проверяется
каждый конец вместе с соседями на расстоянии `eps`: там стоит разница между `≤`
и `<`, а вместе с ней балл. Значение, о котором численно судить нельзя,
пропускается, и число пропусков печатается.

`language(code)` переключает язык сообщений всех проверок между `'ru'`
и `'en'`. По умолчанию русский; английский просит сам ноутбук в установочной
ячейке. Стережёт [`practicum/tests/check_language.py`](practicum/tests/check_language.py)
с двух сторон — статическим проходом по `ast` и прогоном каждой печатающей
функции в режиме `en`.

`trigger_check(answers, key)` проверяет тренажёр распознавания, называя номера
неверных пунктов, но не верные ответы.

Все проверки распознают незаполненный ответ и печатают `⬜` вместо падения:
ноутбук обязан проходиться сверху вниз и с пустыми заданиями, иначе его нельзя
ни запустить целиком, ни залить туда, где ячейки исполняются автоматически.

Проверено, что механика ловит типовые ошибки: потерянный минус, потерянное
слагаемое при подстановке `y = vx`, лишний шаг Эйлера, деление на `cos x`,
область в один оборот вместо двух — все дают `❌`.

## E7, дифференциальные уравнения

[`practicum/calculus/practicum-e7-differential-equations.ipynb`](practicum/calculus/practicum-e7-differential-equations.ipynb)
— 48 ячеек, 254 балла архива, [карточки приёмов](practicum/skills/calculus-differential-equations.yaml).
Тема выбрана первой потому, что даёт самую чистую лестницу во всём архиве:

| Приём | Триггер | Сводится к |
| --- | --- | --- |
| 1 Прямое интегрирование | справа нет `y` | — |
| 2 Разделение переменных | правая часть распадается в `f(x)g(y)` | 1 |
| 3 Разделение + простейшие дроби | знаменатель раскладывается на множители | 2 |
| 4 Однородное, `y = vx` | правая часть зависит только от `y/x` | 2 или 3 |
| 5 Интегрирующий множитель | приводится к `y' + P(x)y = Q(x)` | — |
| 6 Метод Эйлера | дан шаг `h`, нужно приближение | — |
| 7 Знак ошибки Эйлера | «занижено или завышено» | 6 |

Приёмы 1–4 — одна ветка, где каждый следующий буквально сводит задачу к
предыдущему. Это тот случай, когда лестница не придумана ради методики, а
существует в самой математике.

Одиннадцать заданий взяты из сессий 2021–2025 без изменений: от прямого
интегрирования (November 2021 P1 Q1, 4 балла) до связки Эйлер + однородная
подстановка + простейшие дроби в одном вопросе (May 2022 TZ1 P2 Q12, 19 баллов).
Задание на таймере — November 2022 P1 Q9, 10 баллов, норматив 15 минут
из расчёта 1,5 минуты на балл.

**Известное отклонение.** `check_map.py` показывает E7 как 275 баллов при верхней
границе 200. Естественный разрез — после приёма 5, между аналитическими методами
и численными. Он не сделан сознательно: в Paper 2 обе половины стоят в одном
вопросе — сначала Эйлер, потом точное решение, потом сравнение, — и разрез убил бы
связку, ради которой вопрос составлен. Задание 10 в прототипе и есть эта связка.
Решение записано в `map.yaml` полем `exception` и подлежит пересмотру, если
ноутбук перестанет проходиться за один заход.

## C3, тригонометрические тождества и уравнения

[`practicum/geometry/practicum-c3-trigonometric-equations.ipynb`](practicum/geometry/practicum-c3-trigonometric-equations.ipynb)
— 69 ячеек, 162 балла архива, [карточки приёмов](practicum/skills/geometry-trigonometric-equations.yaml).

Самая «бумажная» тема архива: доля баллов с калькулятором 23%, ниже нет ни у кого.
Лестница здесь устроена иначе, чем в E7. Приёмы 1–7 отвечают на вопрос
«как свести уравнение к простейшему»:

| Приём | Триггер | Сводится к |
| --- | --- | --- |
| 1 Опорный угол | одна функция, аргумент равен `x` | — |
| 2 Составной аргумент | под функцией `ax + b` | 1 |
| 3 Сложение углов | нужно точное значение угла не из таблицы | 1 |
| 4 Пифагорово тождество | квадрат одной функции и первая степень другой | 1 |
| 5 Двойной угол | в уравнении есть и `2x`, и `x` | 4 |
| 6 Разложение на множители | общий множитель, или вид `A·B = 0` | 5 |
| 7 Сведение к тангенсу | `sin` и `cos` в одинаковой степени | 6 |

А приём 8, отбор корней, отвечает на другой вопрос — какие из найденных корней имеют
право остаться — и потому применяется **поверх** любого из первых семи, а не после
седьмого. Девятый, численное решение, стоит сбоку: он для Paper 2.

### Тождества: два направления одного приёма

Тема `geometry.trigonometric_identities` не стала отдельным практикумом, и это
решение стоит объяснить, потому что оно про правило гранулярности.

На тождества приходится 53 балла — ниже нижней границы 60. Но дело не только
в объёме: разбор всех шестнадцати блоков показал, что это не тема, а стык.
Четыре блока (18 баллов) получают тождество возведением в степень по Муавру
и приравниванием мнимых частей — приём там комплексный, тождество лишь результат,
и место им в A6. Оставшиеся двенадцать почти целиком разворачивают приёмы,
которые в C3 уже есть: `pythagorean_reduction` и `double_angle_reduction`,
только в направлении «докажи» вместо «реши».

Отсюда правило, которого раньше в этом документе не было: **прежде чем резать
тему на практикумы, проверьте, не является ли она обратным ходом уже описанного
приёма.** Если да, она не новый практикум, а вторая половина карточки — поле
`reverse`. Экзамен спрашивает оба направления, причём в одном вопросе:
«show that» стоит пунктом (a), решение уравнения — пунктом (b).

Единственное настоящее добавление — формулы сложения углов `sin(A ± B)`,
`cos(A ± B)`, `tan(A ± B)`: в уравнениях они не встречались. Под них заведён
приём 3 и задания Т2, Т5.

В ноутбуке два трека номеров: **1–12** решают уравнения, **Т1–Т5** доказывают
тождества. Тождество стоит рядом с той теорией, чей приём разворачивает.
Раздельная нумерация выбрана сознательно: она не сдвигает уже опубликованные
номера и сразу говорит, чего ждать от задания.

Проверять «show that» пришлось иначе. Ответ там напечатан в условии, прятать
его бессмысленно, поэтому `verify_identity` проверяет не ответ, а переход:
студент вводит промежуточное выражение, а функция убеждается, что оно
тождественно равно исходному. Символьного `simplify` для тригонометрии
не хватает — он часто не доводит верную разность до нуля, — так что за ним идёт
численная проверка в восьми точках с пропуском особых. Побочная выгода:
проверка принимает любую верную выкладку. В Т3 «неудобная» форма `cos 2x`
даёт то же тождество через лишний шаг, и она засчитывается — это отдельным
тестом закреплено.

Тема потребовала нового вида проверки. В E7 ответ это функция, и её достаточно
подставить в уравнение. Здесь ответ — множество корней, и подстановки мало:
самая частая потеря баллов выглядит как «все выписанные корни верны». Поэтому
`verify_roots` дополнительно сканирует отрезок и сравнивает число найденных корней
с числом заявленных.

**Находка в разметке.** Проверяя задание про `arccos x + arccos 3x = 3π/2`,
обнаружил, что в `method_path` корпуса записан ответ `x = ±1/2`. Он неверен: при
таком `x` величина `arccos 3x` не определена, поскольку `|3x| = 1,5 > 1`. Настоящее
решение единственно, `x = −1/√10`; второй корень квадрата, `+1/√10`, посторонний и
даёт сумму `π/2`. Это и ещё несколько наблюдений записаны в карточках приёмов
в разделе `corpus_issues` — их стоит перенести в `classification/reviews`.

## C4, синусоидальные модели

[`practicum/functions/practicum-c4-sinusoidal-models.ipynb`](practicum/functions/practicum-c4-sinusoidal-models.ipynb)
— 55 ячеек, 83 балла архива, [карточки приёмов](practicum/skills/functions-trigonometric-models.yaml).

Зеркало C3, и полезно именно как зеркало. Там 77% баллов без калькулятора,
здесь 87% с ним. Там ответ — множество корней и главная ошибка «нашёл не все»,
здесь ответ — одно число с тремя значащими цифрами, а главная ошибка
происходит на входе, при переводе слов в формулу.

Вся тема — одна формула `H(t) = a·sin(b(t − c)) + d` и восемь приёмов вокруг неё:
пять собирают модель, три ей пользуются.

| Приём | Триггер | Даёт |
| --- | --- | --- |
| 1 Период и коэффициент | «период», «между приливами» | `b` |
| 2 Амплитуда и средняя линия | даны максимум и минимум | `a`, `d` |
| 3 Знак амплитуды | «начинает движение в нижней точке» | знак `a` |
| 4 Фазовый сдвиг | известен момент первого максимума | `c` |
| 5 Сборка модели | описание словами, нужно всё сразу | всё |
| 6 Чтение модели | «высота в 12:00», «сколько раз» | значение |
| 7 Порог и суммарное время | «сколько часов выше 5 метров» | интервал |
| 8 Оценка через ±1 | «докажите, что всегда выше» | неравенство |

**Раздел про калькулятор здесь перевёрнут.** В C3 он объясняет, что меняется,
когда калькулятор появляется. В C4 калькулятор есть почти всегда, поэтому
интереснее оставшиеся 11 баллов Paper 1 — и оказалось, что они устроены
принципиально иначе. В Paper 1 **ни разу** не просят найти параметры модели:
арифметику проверяют в Paper 2. Вместо этого спрашивают период в буквах
(`m = π/2q`), чётность, оценку через ±1. Практический вывод для подготовки:
в этой теме не надо тренировать счёт руками, надо тренировать рассуждение
о параметрах в общем виде.

**Приём 8 — единственный, где калькулятор бесполезен.** «Докажите, что растение A
при t > 6 всегда выше растения B» через численные корни не доказывается вообще.
Берут разность `sin(2t + 6) + t − 5`, замечают, что при t > 6 второе слагаемое
больше единицы, а синус не меньше −1, и всё. Форма рассуждения одна: отделить
синус, оценить его константой, показать, что остального хватает с запасом.

**Что приёмом не стало.** Чётность (4 балла) и композиция с тригонометрией
(4 балла) в тему попали по разметке, но своего приёма не образуют: один шаг,
два блока каждая. Записаны в `leftovers` карточки. Композиция при этом
показательна — оба блока это пункт (a) вопроса, чей пункт (b) уже разбирается
в C3: сначала `f(2x) = 3sin2x/cos2x`, потом решают `tan 2x = 1/√3`.

**Проверка считает ответы заново.** В `verify_c4.py` модель прилива собирается
из условия задачи, а не берётся из решения: амплитуда, средняя линия, `b` и `c`
вычисляются, потом проверяется, что максимум встал туда, где сказано в условии,
и только затем числа идут в проверки. Так арифметическая ошибка в разделе
решений не сможет совпасть сама с собой. Заодно оба способа markscheme
для задания на таймере считаются независимо и сверяются друг с другом.

**Находка в разметке.** В evidence блока 2025-MAY-TZ1-P2-Q02-B markscheme
приведён как «a = -√3/2 (0.866025..., 0.866)» — в десятичном пояснении потерян
минус. Решение системы даёт −0.866; тест это проверяет независимо.

## A5 и A6, комплексные числа

[`practicum/number_algebra/practicum-a5-complex-forms.ipynb`](practicum/number_algebra/practicum-a5-complex-forms.ipynb)
— 48 ячеек, 144 балла, [карточка](practicum/skills/number-algebra-complex-forms.yaml).
[`practicum/number_algebra/practicum-a6-de-moivre.ipynb`](practicum/number_algebra/practicum-a6-de-moivre.ipynb)
— 53 ячейки, 112 баллов, [карточка](practicum/skills/number-algebra-de-moivre.yaml).

**Как поделена тема.** В карте у A5 и A6 стояла доля 0.5/0.5, поставленная
на глаз. Я разнёс все 69 блоков темы поимённо и получил 144 и 94 балла
(плюс 18 баллов тождеств у A6), то есть 0.605 и 0.395. Списки блоков лежат
в карточках, так что разбиение теперь проверяемое, а не декларативное.

Граница проходит по существу приёма: **появилось `z^n` — это A6.** Модуль,
аргумент, сопряжённое, приравнивание частей — A5. Формулировка короткая,
но работает: ни один блок не оказался спорным.

**A5 держится на одной идее** — свободно переходить между тремя записями числа.
Шесть приёмов: арифметика и сопряжённое, приравнять части, модуль и аргумент,
перевод форм, свойства произведения, сопряжённые корни многочлена. Правило,
экономящее половину работы, вынесено в карту приёмов: складывают в декартовой,
умножают в полярной.

**A6 держится на одной формуле** — `(re^{iθ})^n = r^n e^{inθ}`. Все шесть приёмов
там это её прочтения в разные стороны: степень применяет напрямую, корни
обращают, корни из единицы — частный случай с сильной симметрией, условие
периодичности смотрит только на аргумент, поворот читает умножение как движение
плоскости, тождества раскрывают степень дважды и приравнивают результаты.
Именно поэтому 18 баллов тождеств из C3 приехали сюда: там это шестое прочтение
той же формулы, а не тригонометрия.

### Комплексные числа сломали проверку, и её пришлось чинить

`check_expr` сверяет хеш канонической записи через `srepr(simplify(...))`.
Для комплексных чисел это не работает: sympy упрощает `2e^{2πi/3}`
до `2·(−1)^{2/3}`, а `−1 + √3 i` оставляет как есть, и хеши расходятся.
**Верный ответ в полярной форме получил бы ❌ против декартова эталона** —
ровно та ошибка, которой вся конструкция kit должна избегать.

Добавлены `check_complex` и `check_complex_set`: они сверяют **само число**,
а не запись, округляя действительную и мнимую части до заданного числа
значащих цифр. Параметр `sf` заодно задаёт строгость — 6 значит «нужна точная
форма», 3 значит «достаточно трёх цифр, как в Paper 2».
Механика закреплена в [`test_kit_complex.py`](practicum/tests/test_kit_complex.py):
23 проверки, из которых восемь негативных.

**Чего проверка не ловит и не может.** Когда просят аргумент в промежутке
`(−π, π]`, за приведение к нему стоит отдельный балл. Но `e^{13iπ/12}`
и `e^{−11iπ/12}` — одно и то же число, и различить их сверкой по значению
нельзя. В ноутбуке A6 об этом сказано прямо, а в тестах поведение зафиксировано
отдельной проверкой, чтобы не выглядело недосмотром.

### Находка в разметке

Блок 2025-NOV-TZ1-P1-Q11-D: в `method_path` записано `a² = (√3 + 1)/2`.
Неверно. Из `(a + bi)² = √3 + i` следует `a² − b² = √3` и `2ab = 1`, а модуль
даёт `a² + b² = 2`, откуда `a² = (2 + √3)/2 ≈ 1.866`. Значение из разметки,
1.366, даёт `2ab = 1.861` вместо единицы.

Ошибка внутри самого `method_path`: там написано
`a² = (4√3 ± √(48+16))/8 = (√3 ± 1)/2`, но `√64 = 8`, и выражение равно
`(4√3 ± 8)/8 = (√3 ± 2)/2`. В `verify_a5.py` оба пути markscheme считаются
независимо и сверяются друг с другом, а в негативных проверках стоит
значение из корпуса — оно обязано давать ❌.

## A3, биномиальная теорема

109 баллов, 24 блока, семь приёмов. Тема взята целиком и оказалась самой
однородной из построенных: **шесть приёмов из семи — прочтения одной формулы**

```
T_{r+1} = C(n,r) · a^(n−r) · b^r
```

Приём 1 (раскрыть целиком) — её частный случай, когда членов мало.
Приём 3 применяет её дважды и сравнивает, приём 4 — трижды и смотрит
на связь, приём 5 продолжает на нецелые `n`, приём 6 перемножает два
таких ряда, приём 7 подставляет в неё `i`.

Отсюда главное умение темы, вынесенное в шапку ноутбука: **не раскрывать**.
Вопрос почти всегда про один член, и ищут не разложение, а номер `r`.
Ученик, который раскрывает `(ax+1/x²)^9` целиком, не ошибается — он просто
не успевает.

Единственная тема секции A без единого блока Paper 3: вопросы короткие
и самостоятельные, в длинные исследования бином не заходит. Paper 1 — 77
баллов, Paper 2 — 32.

### Разложения нельзя сверять по записи

Пришлось добавить `check_series`. Причина в трёх независимых местах, где
сравнение канонических форм ломается:

- ученик пишет `5*x/2`, Python при делении даёт `2.5`, а `srepr`
  у `Rational(5,2)` и `Float(2.5)` разный;
- `simplify` не разворачивает `(1+x)**4` в многочлен и не сворачивает
  многочлен обратно — какая из форм «каноническая», не определено;
- тригонометрический ответ `cos⁵θ − 10cos³θsin²θ + 5cosθsin⁴θ` имеет
  как минимум три равноправные записи, и markscheme принимает все.

`check_series` сверяет **значения в пяти фиксированных точках**. Разные
многочлены в пяти точках не совпадают, а одинаковые совпадают при любой
записи. Проверено в `test_kit_series.py`: sympy выдал Re и Im в свёрнутом
через пифагорово тождество виде, отличном от того, из которого считался
эталон, — и проверка их приняла.

**Чем платим.** Нераскрытая скобка численно равна своему разложению,
и `(1+x)**4` проверка примет, хотя в условии сказано «expand». Отличить
их по значениям нельзя в принципе. То же с `cos(5θ)` вместо развёрнутой
формы — на экзамене за такой ответ баллов не дадут. Оба случая записаны
в `verification_limits` карточки и оговорены в ноутбуке.

### Заодно починена латентная ошибка в `check_set`

`_blank` искал многоточие только на верхнем уровне, а в ячейке с ответом-набором
стоит `my = [...]` — список, снаружи неотличимый от заполненного. `check_set`
на нём падал с `SympifyError`, то есть ноутбук с незаполненным заданием
не проходился. `check_complex_set` от этого был защищён случайно (там стояло
`_blank(label, values, *values)`), остальные — нет.

Починено в самом `_blank`: он теперь рекурсивно смотрит внутрь списков,
кортежей и множеств. Это закрывает все места разом и ничего не ломает —
поведение меняется только в сторону «ловим больше пропусков».

### Три находки в разметке

**`2023-MAY-TZ1-P2-Q05`** — в `method_path` коэффициент при `x^6` записан
как `C(8,2)·a^6·b²`, и та же запись повторена в `evidence`. Степени
перепутаны: `x^6` приносит только `ax³`, взятый дважды, поэтому коэффициент
равен `C(8,2)·a²·b^6`. Система из записи корпуса даёт `a = 2, b = 1/2` —
ровно перевёрнутый ответ. При этом итог в корпусе (`a = 1/2, b = 2`) верен
и с собственной выкладкой не согласуется.

**`2025-MAY-TZ2-P1-Q11-D`** — цель приближения записана как `sqrt(5/2)`,
а читать надо `√5/2`. Markscheme даёт `x = 1/20`, что отвечает `1+5x = 5/4`,
то есть `√1.25`. Для `√2.5` понадобилось бы `x = 3/10`, а это вне области
сходимости `|x| < 1/5` — и ответом было бы не «менее точно», а «разложение
неприменимо». Различать эти два случая полезно само по себе, поэтому
в решении разобраны оба.

**`2022-NOV-COMMON-P2-Q05`** — блок помечен `formula_extraction_uncertain`,
и разбор внутренне противоречив: в `(ax + 1/x²)^9` члена с `x^4` не существует
(`9−3r = 4` даёт `r = 5/3`), а правая часть уравнения указана двумя разными
способами, дающими разные ответы. Блок как задание не используется.

## A7, доказательства

160 баллов, 28 блоков, девять приёмов. Две темы корпуса взяты целиком —
индукция (112 баллов) и proof (48). Резать не пришлось: в диапазон
укладывается, а делить нечем. Шаг индукции сам является прямым
доказательством, а индукция для делимости построена на представлении
целых из темы proof, так что разрез прошёл бы поперёк связей.

Лестница идёт от простого к сложному в буквальном смысле: сначала три
приёма без индукции (прямое, представление целых, от противного), потом
скелет индукции, потом пять её видов, различающихся ровно одним — что
делают с гипотезой. Прибавляют слагаемое, вычитают кратное, дифференцируют,
подставляют в рекуррентность, продолжают неравенство. Всё остальное
в них совпадает до последней запятой.

Единственная тема архива, где калькулятор не нужен нигде. Формально доля
с GDC 34%, но разложение по бумагам объясняет цифру целиком: Paper 1 —
106 баллов, Paper 3 — 48, Paper 2 — 6. Все 48 баллов Paper 3 это части
длинных исследований, где GDC работает в соседних пунктах, а доказательство
пишут от руки. Раздел «Где здесь калькулятор» в ноутбуке перевёрнут
так же, как в C4, но по обратной причине.

### Тема, где проверять нечего — и что с этим делать

Во всех прочих практикумах ответ надо найти, и его прячут под хеш. Здесь
ответ **напечатан в условии**: «prove that», «show that». Прятать нечего,
и хешей в A7 всего два — на порядок строк и на количество баллов.

Проверять при этом есть что, просто не итог:

- **переход.** `verify_induction` берёт то, что вы получили в шаге, и сверяет
  с утверждением при `k+1`. Заодно проверяет базу, если её передали, —
  в markscheme это отдельный балл;
- **остаток.** `verify_residue` перебирает целые значения всех символов
  и убеждается, что остаток от деления постоянен. Этим закрывается почти
  вся некомбинаторная половина темы: «делится на 3» — остаток 0, «никогда
  не делится» — остаток 2, «чётно» — mod 2;
- **делимость как выражение.** `verify_divisibility` требует, чтобы после
  деления на `d` остались **целые коэффициенты**. Требование не формальное,
  см. ниже;
- **порядок шагов.** `check_order` — новая проверка под задание, где
  доказательство собирают из перемешанных строк.

А вот словесную часть — два-три балла из семи в каждом задании темы —
ноутбук не проверит никогда. Поэтому в каждом решении приведена
формулировка markscheme дословно, и в ноутбуке про это сказано прямо,
а не спрятано в оговорку.

### Почему делимость проверяется по коэффициентам, а не по значениям

Место, которое чуть не прошло незамеченным. Доказываем, что `5^{2n} − 2^{3n}`
делится на 17. В шаге из `E(k+1)` вычитают кратное гипотезы: с множителем 8
остаётся `17·5^{2k}`, с множителем 25 — `17·2^{3k}`, оба пути markscheme
даёт как EITHER/OR.

Возьмите множитель 1. Остаток `24·5^{2k} − 7·2^{3k}` **тоже кратен 17**
при каждом целом `k` — потому что `25 ≡ 8 (mod 17)`, и так будет с любым
множителем вообще. Проверка «делится ли остаток на 17 численно» пропустила
бы всё что угодно, то есть была бы бесполезна.

Работает другое: после деления на 17 должны остаться целые коэффициенты.
При множителе 1 остаются `24/17` и `−7/17`, значит 17 из записи не вынести,
и доказательства нет. A1 в markscheme стоит именно за вынесение `d`
за скобку — проверка сверяет ровно то, за что дают балл.

### Чего проверка не ловит

Одна буква на два независимых целых. Запись `2(2m+1)²` даёт остаток 2
по модулю 4 ровно так же, как честная `(2m+1)² + (2n+1)²`: для частного
случая `a = b` утверждение верно, неполно здесь **рассуждение**, а полноту
рассуждения из остатков не вычислить.

Мой негативный тест на этот случай сначала утверждал обратное и провалился —
неправ был тест, а не код. Заменён на положительный, фиксирующий поведение;
в ноутбуке и в карточке сказано прямо.

### Находка в разметке

Блок 2021-MAY-TZ1-P1-Q12-B: в `method_path` формула `n`-й производной
записана как `((−1)^(n−1)/4)·((2n−3)!/(n−2)!)·(1+x)^(−(2n−1)/2)`.
Константа 4 неверна — на её месте должно стоять `2^(2n−2)`.

Ошибка живучая: формула совпадает с истинной производной **ровно при n = 2**,
то есть в базовом случае, который процитирован в evidence. Дальше расходится:
при `n = 3` даёт `3/2` вместо `3/8`, при `n = 4` — `15` вместо `15/16`.
Там же приведено «тождество» `(2k−1)!/(k−1)! = 2(2k−3)!/(k−2)!`, у которого
отношение сторон равно `2k−1`, то есть равенства нет.

На практикум не влияет: приём отрабатывается на блоке 2021-NOV-COMMON-P1-Q11-A
с формулой `(x²+2nx+n(n−1))e^x`, которая сверена с настоящими производными
sympy при `n = 1…5`.

## A4, многочлены

145 баллов, 50 блоков, девять приёмов. Самая широкая тема секции A
и единственная в архиве, где **Paper 3 весит больше Paper 2**: 62 балла
в Paper 1, 34 в Paper 2, 49 в Paper 3.

Приёмы легли в две половины, и это оказалось главной мыслью темы.
Многочлен читается с двух сторон, и вся тема — переходы между ними:

- **коэффициенты → корни** (приёмы 1–3): подставили, нашли множитель,
  поделили, дошли до кратного корня;
- **корни → коэффициенты** (приёмы 4–8): корни не находят вовсе, работают
  с их суммами и произведениями, потому что те выражаются через
  коэффициенты сразу.

Отсюда рабочее умение, вынесенное в шапку: **до начала счёта посмотреть,
спрашивают ли корни**. «Найдите произведение решений», «покажите, что
`a² − 3b = 0`», «найдите `q` и `r`» решаются, не находя ни одного корня.
Кто начинает с формулы корней кубического уравнения, теряет не баллы,
а время.

Девятый приём — простейшие дроби — стоит особняком: он ничего не говорит
о корнях, зато превращает дробь в сумму, которую можно проинтегрировать.
Все четыре его блока в архиве — пункты (a) длинных вопросов по
интегрированию, поэтому в карточке он ведёт в E5 и E7.

### 57% с калькулятором и почему это неправда

Формально 83 балла из 145 — в бумагах с GDC, ровно середина архива.
Реальной работы для калькулятора здесь **на четыре балла**: перебрать
целые `q` в исследовании мая 2022 и решить систему из трёх линейных
уравнений в ноябре 2022. Всё остальное — алгебра, одинаковая в обеих
бумагах.

Причина в устройстве темы. Калькулятор умеет находить корни, а вопросы
здесь про то, что связано с корнями **без их нахождения**. Уравнение
`x³ − 7x² + qx + 1 = 0` с буквой `q` GDC не решит, а Виета — за строку.

Тот же перекос, что в A7, но по другой причине: там 48 из 54
«калькуляторных» баллов оказались Paper 3, здесь калькулятор просто
не подходит к вопросу.

### Проверка здесь обратная к A3

Самое интересное инженерное место практикума. В A3 `check_series` сверяла
значения, и нераскрытая скобка проходила — потому что «expand» это просьба
к записи, а не к числу, и по значениям её не отличить. Записано в
`verification_limits` как принятая цена.

В A4 то же самое требование стоит с другой стороны: **«express as a product
of three linear factors» — это и есть задача**. Ответ `3x³+5x²+x−1` равен
правильному, но балла не получает: разложения в нём нет. Значение сверять
бессмысленно, сверять надо форму.

Поэтому добавлены пять проверок, и две из них разбирают структуру записи:

- **`verify_factored`** — разбирает **написанное произведение**, не факторизуя
  его. `sp.factor_list` разложил бы и раскрытый многочлен, и проверка стала
  бы бессмысленной; вместо этого берётся `sp.Mul.make_args`, каждый множитель
  проверяется на степень, кратные считаются по разу за каждую степень.
  Потом отдельно — равенство исходному.
- **`check_apart`** — то же для простейших дробей. Равенства мало: исходная
  дробь равна сама себе. Каждое слагаемое обязано быть простейшей дробью —
  числитель без переменной, знаменатель степень **одного** неприводимого
  множителя. Знаменатель `(x+1)(2x+1)` проверку не проходит.

Остальные три вообще не хранят эталона:

- **`verify_division`** — восстанавливает делимое из частного и остатка
  и отдельно требует `deg R < deg d`. Ответ «частное `2x`, остаток `−3x−3`»
  арифметически верен, но делением не является.
- **`verify_divisible`** — подставляет найденные буквы в многочлен и считает
  настоящий остаток от деления. Для задания «найдите `p` и `q`, при которых
  делится на `(x+1)²`» проверяется ровно то условие, что стоит в задаче.
- **`verify_root_transform`** — считает корни исходного уравнения численно,
  применяет к ним преобразование и сравнивает с корнями предложенного.
  Побочно и правильно: уравнение, умноженное на константу, тоже проходит —
  корни у него те же, и markscheme его принимает.

В `test_kit_poly.py` последним блоком стоит прямое сравнение: один и тот же
ответ `(x+1)²(3x−1)` и его раскрытая форма неразличимы для `check_series`
и различимы для `verify_factored`. Обе проверки правы — просто просили
разное.

### Три находки в разметке

**`2021-MAY-TZ2-P1-Q07`** — уравнение записано как `x³ − kx² + 3k = 0`,
и с ним записанный там же ответ `k = 2√6` несовместен: последний шаг
разбора превращает `k³/8 + 3k = 0` в `k²/8 = 3`, теряя знак. В sympy
`k³/8 = −3k` даёт `{0, ±2√6·i}` — положительных действительных решений
нет. Кроме того, данное в условии `αβ = k²/4` несовместимо с нулевым
коэффициентом при `x`: сумма попарных произведений равна `αβ + (α+β)²`.
В практикуме уравнение приведено к согласованному виду
`x³ − kx² + (k²/2)x − 3k = 0`, где все три формулы Виета сходятся и дают
ровно записанный в корпусе ответ.

**`2025-NOV-TZ3-P2-Q06`** — «solve simultaneous equations `r − q = 10`
and `q²/4 − r = 37/4` to find `q = −7, r = 3`». Система квадратная, и решений
у неё **два**: `(−7, 3)` и `(11, 21)`, оба удовлетворяют обоим уравнениям.
Второе отбрасывается только проверкой дискриминанта: у `2x²+11x+21` он
равен `−47`, корни недействительны. Значит, в условии обязано стоять
требование действительных корней — иначе блок недоопределён. Задание
построено вокруг этого: сначала спрашиваются оба кандидата, потом выживший.

**`2024-MAY-TZ1-P1-Q11-D`** — правая часть записана как
`1 − 4/(2x+1) − 2/(x+1) + 2/(2x+1)²`, и левой части она не равна.
Настоящее разложение — `2/(x+1) + 1/(x+1)² − 4/(2x+1) + 2/(2x+1)²`;
разность с записью корпуса равна `(−x²+2x+4)/(x+1)²`. Два слагаемых
из четырёх совпадают, у `2/(x+1)` перевёрнут знак, а на месте `1/(x+1)²`
стоит свободная единица — похоже на потерю знаменателя при извлечении
формулы. Задание от исправления только выиграло: кратный множитель
в знаменателе — единственное место темы, где на один множитель нужны
два слагаемых.

### Что не стало приёмом

Три группы блоков вынесены в `leftovers` карточки, а не в приёмы:

- **упрощение рациональных выражений** (~15 баллов) — сократить дробь,
  привести к общему знаменателю, выбрать корень после возведения в квадрат.
  Фоновый навык, а не приём темы; в корпус попал под `polynomials`, потому
  что работа идёт с многочленами;
- **подгонка многочлена под данные** (5 баллов) — `Σi² = a₁n + a₂n² + a₃n³`
  подстановкой `n = 1,2,3`. По существу принадлежит A1 и A7;
- **координатная геометрия внутри исследования** (6 баллов) — подставить,
  найти наклон, посчитать отношение. Принадлежит B3 и C5.

## A8, неравенства

87 баллов, 31 блок, восемь приёмов. Самая маленькая тема секции A и самая
рассыпанная: **26 блоков из 31 — отдельные пункты внутри чужих вопросов**,
чаще всего последние. Целым вопросом неравенство за пять лет бывает дважды.

Главная мысль темы записана в шапке практикума одной фразой: **у неравенства
нет ответа-числа, ответ — это множество, и все баллы стоят на его границе.**
Конец, пришедший из нуля выражения, входит тогда и только тогда, когда
неравенство нестрогое; конец, пришедший из разрыва, не входит никогда.
Всё остальное в теме — способы эту границу найти.

Лестница построена по тому, **где живёт ответ**:

- приёмы 1–4 — неравенство **решают**, ответ множество по `x`
  (критические значения, дробь, модуль, GDC);
- приёмы 5–7 — неравенство **получают** как условие на букву («два
  различных корня» это `Δ > 0`), ответ множество по параметру, а решается
  оно теми же приёмами 1–4;
- приём 8 — неравенство **доказывают**, множества нет вовсе.

Практическое следствие, ради которого лестница так и устроена: до начала
счёта надо понять, о чём спрашивают — о `x` или о букве. Половина потерянных
баллов в теме это перепутанные роли.

### Третье понятие равенства ответов

Тема оказалась интересной не задачами, а проверками. В A3 сверялись
**значения**, и нераскрытая скобка проходила. В A4 сверялась **форма
записи**, потому что «представьте произведением линейных множителей» —
требование к записи. Здесь ответ — **множество**, и сверять надо множества.

Разница не косметическая. Именно множество различает `−5 < x < 1`
и `−5 ≤ x ≤ 1`, а это разные баллы markscheme. Числовая проверка их
не отличит никак: значения-то одни и те же.

В `kit.py` добавлены три проверки.

**`verify_solution_set`** решает неравенство сам (`solveset` по самому
условию, напечатанному в задании) и сравнивает множества точно, вместе
с концами. Эталон не хранится. Сужать ответ областью нельзя: «`d < 0`
или `d > 9`, но `d ∈ ℝ⁺`, поэтому `d > 9`» — это и есть последний балл
задачи, и потерянное ограничение обязано быть видно как лишний кусок
ответа.

**`verify_param_set`** не решает ничего. Она берёт ваше множество и
спрашивает у самого условия: внутри — выполняется ли свойство, снаружи —
не выполняется ли. Точки берутся внутри каждого промежутка, в каждой
дырке, на каждой границе и по обе стороны от неё. Условие пишется прямо
в ячейке задания и говорит ровно то, что спрашивает вопрос: «сколько
у уравнения различных действительных корней», «какого знака корни»,
«чему равен максимум». **Дискриминант в проверке не участвует вовсе** —
поэтому она и ловит вырожденный случай `p = 0`, где уравнение перестаёт
быть квадратным, а формула `b² − 4ac` молча врёт.

**`verify_nonneg_form`** требует, чтобы неотрицательность была видна
из записи: сумма квадратов, модулей и неотрицательных чисел, без
слагаемых со знаком минус. Раскрытый `x² − 2xy + y²` она не примет, хотя
он равен `(x − y)²`. Это то же требование к форме, что `verify_factored`
в A4, — там просили произведение, здесь просят квадрат, и в обоих случаях
за это стоит M1.

### Чему научил один провал теста

Первая версия `verify_param_set` брала пробные точки только внутри
промежутков вашего ответа и в дырках между ними. Тест «выколотая точка
не выколота» — ответ `m > 0` вместо `m > 0, m ≠ 1` — она **прошла**,
хотя должна была провалить.

Причина простая и общая: дефект в одной точке не имеет внутренности,
и ни одна проба, привязанная к промежуткам, в него не попадает.
Между тем это самая частая ошибка приёма `solution_count`: значение,
при котором два решения слились в одно, из ответа «ровно два» выбрасывают
не все, и за это стоит отдельный балл.

Лечится регулярной сеткой поверх интервальных проб — шаг подбирается так,
чтобы узлов было около восьмидесяти на окно. Целые точки в неё попадают
всегда, а границы такого рода почти всегда целые. Заодно стало видно, что
у выборочной проверки есть честный предел: **всё, что лежит за окном,
не проверяется**. Ответ `6 < k < 100` вместо `k > 6` пройдёт. Это
записано и в `verification_limits` карточки, и отдельным тестом
в `verify_a8.py`, чтобы предел был измерен, а не подразумевался.

### Иллюзия калькулятора, третий раз

Формально 49 баллов из 87 — с калькулятором, 56%. Реальной работы для GDC
в теме на **девять баллов**: одно неравенство с показательной функцией
и дробью, один целочисленный ответ и одно чтение по графику в исследовании.
Остальное в «калькуляторных» бумагах — дискриминант, Виета и доказательство
от противного, которое GDC не сокращает ни на строку.

Зато у единственной честно калькуляторной задачи темы интересное
устройство: **три балла из четырёх стоят за запись, а не за счёт.**
Калькулятор выдаёт три точки пересечения за десять секунд; работа
экзаменуемого в том, чтобы понять, где неравенство выполняется, и какие
концы входят. В ответе рядом стоят конец-пересечение (входит) и
конец-полюс (выколот), и различать их надо каждый раз заново.

### Что нашлось в корпусе

Четыре расхождения, все перепроверены в sympy и записаны в `corpus_issues`
карточки. В `classification/` ничего не менялось.

**`2022-MAY-TZ1-P2-Q06-B`** — записано `f(x) = 2^(−x) − 2^x`. С такой `f`
ни одна из трёх записанных там же точек пересечения не является решением:
в них получается `f = −g`, а не `f = g`. При `f(x) = 2^x − 2^(−x)` все три
точки обращают разность в ноль, и знак на шести промежутках воспроизводит
записанный ответ целиком, вместе с выколотыми `−1` и `3`. Перепутан знак
при извлечении, а не ответ.

**`2025-NOV-TZ3-P2-Q09`** — в выдержке из markscheme стоит
`0 > x² − 4xy + y²`, а следующей строкой `0 > (x − y)²`. Одно из другого
не следует: возведение в квадрат даёт `0 > x² − 2xy + y²`, а `x² − 4xy + y²`
полным квадратом не является вовсе (`factor` оставляет его как есть).
Строка про `(x − y)²` верна и согласуется именно с `−2xy`.

**`2022-MAY-TZ2-P1-Q03-D`** — у блока стоит собственная пометка корпуса
`formula_extraction_uncertain`, и не зря. В выдержке речь об эскизе `|f(x)|`,
а записанный ответ `x < −1/2` или `x > 1/2` отвечает `f(|x|) > 0`: через
`|f(x)|` его получить нельзя, `|f(x)| > 0` означает всего лишь `x ≠ 1/2`.
Задание поставлено как `f(|x|) > 0`, а разница между `f(|x|)` и `|f(x)|`
вынесена в теорию отдельным абзацем — она и есть содержание приёма.

**`2025-NOV-TZ1-P3-Q01-B-I`** — в кратком описании блока формула записана
как `c = 1/m^(1/2) − 1/m^(1/2)`, то есть тождественный ноль. В выдержке
из markscheme в том же блоке стоит `c = ±(1/√m − m√m)`, и с ней сходится
всё, включая слияние двух значений при `m = 1`.

### Что не стало приёмом

Три блока, 12 баллов из 87, к неравенствам не относятся вовсе: две системы
линейных уравнений (`2025-NOV-TZ1-P1-Q06`, `2025-NOV-TZ3-P2-Q11-B-II`)
и алгебраическое «show that» без единого знака неравенства
(`2025-NOV-TZ1-P3-Q01-B-I`). Ещё один балл — область сходимости ряда
`|x| < 1/4`, которая принадлежит A3.

Первые два — те самые блоки, из-за которых в карте предложено завести
`number_algebra.systems_of_equations`. Следующий раздел про это.

## B1, уравнения

Самая большая тема после дифференциальных уравнений: 222 балла, 73 блока,
девять приёмов. И самая незаметная — своим именем вопрос называется редко,
зато уравнение решают почти в каждом.

### Уравнения в условии нет

Главная мысль темы обнаружилась при разборе блоков, а не при составлении
карты. Больше половины баллов стоит **до** того, как начинается решение:
«show that the x-coordinates satisfy `x² − 2dx + 9d = 0`» — четыре балла,
и ни один из них не за корни. «Show that `a = 8/(b−4) + 4`» — семь баллов,
из них два за то, что площадь и периметр вообще записаны по отдельности.

Отсюда лестница из трёх частей. Приёмы 1–2 работают с уже записанным
уравнением: нуль функции, квадратный трёхчлен. Приёмы 3–6 его строят —
из условия задачи, из пересечения графиков, из замены. Приёмы 7–9
переворачивают вопрос: уравнение дано с буквой, и спрашивают не корни,
а сколько их.

Практическое следствие для ученика простое и записано в шапку ноутбука:
прежде чем решать, посмотрите, просят ли решить. «Show that»,
«hence show» и «find the value of k such that» — три разных вопроса,
и корни нужны только в первом.

### Четвёртое понятие равенства ответов

Серия продолжает набирать разные ответы на вопрос «когда два ответа
одинаковы». A3 сверял **значения**: `5*x/2` и `2.5*x` — одно и то же.
A4 — **форму записи**: «в виде произведения линейных множителей» это
требование к записи, и раскрытая скобка его не выполняет. A8 — **множества**:
`−5 < x < 1` и `−5 ≤ x ≤ 1` различаются ровно там, где стоят баллы.

Здесь ответом бывает **само уравнение**, и появилось `verify_equation`.
Два уравнения равны, если одно получается из другого переносом слагаемых
и умножением на ненулевое **число**. Умножение на выражение с буквой —
не равенство: `d(x² − 2dx + 9d) = 0` при `d = 0` превращается в `0 = 0`,
и множество корней уже другое. Проверка это различает и говорит, что
именно произошло.

Забавно, что нужное почти существовало: `verify_implicit` из E7 сравнивает
`F(x, y) = c` с точностью до множителя. Но там множителю разрешено
содержать параметры — для неявного ответа это безобидно, для уравнения
нет. Один и тот же приём с двумя разными допусками — и это оказалась
не мелочь, а как раз то место, где тема отличается от соседних.

### Лишние корни и потерянные

Второе новое требование к проверке. Решение уравнения — цепочка шагов,
и не каждый шаг равносилен:

* возведение в квадрат, умножение на знаменатель, «снятие логарифмов» —
  корни **добавляются**;
* деление обеих частей на выражение с переменной — корни **теряются**;
* область из условия (`x > 4`, `s > 0`, `q ≠ 0`) выбрасывает лишние
  в самом конце.

`verify_root_set` смотрит на список с обеих сторон: подставляет каждый
корень в исходное уравнение (лишние) и решает его сам (потерянные).
Сообщения у этих двух случаев разные, потому что разные и ошибки:
в первом случае ученик поверил преобразованию, во втором — сократил.

Самый ясный пример темы — задание про квадрат, у которого площадь равна
периметру. Вся алгебра там на полстроки, `s² = 4s`, а markscheme прямо
пишет: **A1M1A0**, если в ответе записаны оба значения, `s = 4` и `s = 0`.
Третий балл стоит за то, что нуль не попал в ответ.

Отдельно стоит случай, когда отброшенный корень **законен сам по себе**.
В задании про вершину параболы на прямой получается `q(q − 4) = 0`,
и `q = 0` отбрасывают. Но при `q = 0` вершина действительно лежит
на прямой — корень исключает фраза условия «p and q are non-zero
constants», а не математика. Поэтому область в проверке задаётся
отдельным аргументом: она приходит из условия, а не из уравнения.

### Доля темы, посчитанная заново

В карте у B1 стояло `share: 0.55` — число, поставленное на глаз при
составлении карты. Разбор всех ста блоков темы дал 0.771.

Ошибка была в предположении, что композиция функций весит в теме
уравнений много. На деле её основные баллы лежат в
`functions.inverse_functions` и `functions.graphing`, а сюда попадают
27 блоков (66 баллов) — их и забрал B2.

Отсюда 222 балла и выход за верхнюю границу. Разрезать тему было бы
естественно между «решить» (приёмы 1–6) и «сколько решений» (7–9), но
в архиве эти половины стоят пунктами одного вопроса: (a) show that the
intersection satisfies…, (b) hence find the values of the parameter.
Тот же довод, что у E7, и записан он в `exception` рядом с практикумом.

### Одна бумага, посчитанная дважды

Крупнейшая находка разбора — не в математике, а в корпусе. Ноябрьская
Paper 3 2023 года лежит в `classification/generated` **двумя копиями**:
как `2023-november-tz1` и как `2023-november-tz2`, по 22 блока и 55 баллов
в каждой. В архиве бумага одна: `AA_HL/2023/November/Common/Paper 3`,
своих Paper 3 у зон TZ1 и TZ2 нет.

`check_map.py` дублирование не ловит: он отбрасывает повторы по `id`,
а идентификаторы различаются префиксом зоны. Итог корпуса завышен
на 55 баллов из 4675.

Хуже другое. Два независимых прогона по одной и той же бумаге разошлись
в `primary_topic` у **11 блоков из 22** — ровно половины. Вопрос 1(a)–(c)
про число пересечений кубической кривой с осью в одной копии
`functions.curve_sketching`, в другой `functions.equations`. То есть
дубль заодно даёт бесплатный замер устойчивости классификации, и замер
получился отрезвляющий.

Для B1 прямое последствие скромное: шесть баллов вопроса 2(c) попали
в тему уравнений в обеих копиях и потому посчитаны дважды. Пометка
об этом стоит в шапке ноутбука, в `exception` карты и в `corpus_issues`
карточки. В `classification/` ничего не трогалось.

### Условие, восстановленное по бумаге

У ноябрьского вопроса 2021 года (Paper 1, Q3) в корпусе **нет самого
уравнения**: в описании только «applying change-of-base, power rule and
product/quotient rule … leading to `x^5 = 1/32`», и стоит собственная
пометка `formula_extraction_uncertain`.

Это первый раз, когда пришлось открыть исходный PDF, а не работать
разметкой. Текст вопроса извлекается коряво (формулы разложены
на отдельные глифы), но по координатам символов и по промежуточным
строкам markscheme условие собирается однозначно:

```
log₃ √x = 1/(2 log₂ 3) + log₃ (4x³),  x > 0
```

Проверка сходится с двух сторон: такое уравнение даёт ровно записанный
в markscheme шаг `x = 32x⁶`, ответ `x = 1/2` и никаких других
положительных корней. Всё это перепроверяется в `verify_b1.py`.

Вывод на будущее: `pypdf` стоит держать в окружении. Для блоков
с пометкой `formula_extraction_uncertain` пятнадцать минут работы
с бумагой надёжнее любых догадок по markscheme.

### Иллюзия калькулятора, четвёртый раз

Формально 55% баллов темы с калькулятором. Настоящей работы для GDC —
15 баллов из 222: два численных решения внутри моделей и одно чтение
точки пересечения. Остальное в «калькуляторных» бумагах — это Paper 3,
где считают не числа, а условия на параметр.

Зато в этих пятнадцати баллах нашлась деталь, которой не было в прежних
темах. У задачи про черепаху и прилив решается `w(t) = 0`, и численный
поиск видит смену знака около `t = 6.04`. Корня там нет: знаменатель
`sin(2.37 − πt/8)` обращается в ноль, и это полюс. Настоящий второй
корень — `t = 12.78`. Отличить одно от другого можно по значениям
по сторонам: у полюса они порядка 10³, а не нулевые. Калькулятор
об этом не предупредит, и в решении это сказано прямо.

### Где проверка мягче экзамена

Замерено пять пределов, все воспроизводятся в `verify_b1.py`:

1. `verify_equation` принимает любой числовой множитель: запись
   `17x² − 17(m+1)x + 34 = 0` пройдёт там, где markscheme ждёт
   напечатанный вид.
2. `verify_root_set` сверяет, какие корни найдены, а не как они записаны:
   там, где в условии стоит «exact value», десятичная запись с пятнадцатью
   знаками пройдёт, а экзамен её не примет.
3. `verify_param_set` смотрит в окно (−30, 30): лишний кусок ответа
   за его пределами не проверяется.
4. Пробы не находят **пропущенное** отдельное значение, если оно
   иррационально: в регулярную сетку оно не попадает, а внутренности
   у одной точки нет. Поэтому у задания про касание рядом с пробами
   стоит `check_set` — он про полноту набора. Это прямое продолжение
   истории из A8, где такой же дефект нашёлся в целой точке `m = 1`
   и лечился сеткой; здесь стало видно, что сетка помогает не всегда.
5. Потерянная нижняя граница не ловится, если за ней условие
   не определено: при `k ≤ 0` логарифма нет, проба возвращает None
   и пропускается, поэтому ответ «−30 ≤ k ≤ e^{9/4}» проходит.

Четвёртый и пятый пункты — про одно и то же свойство выборочной
проверки: она не может увидеть того, чего не с чем сравнить. Там, где
это важно, рядом ставится вторая проверка другого типа. Два разных
вопроса к одному ответу дешевле, чем попытка сделать один универсальный.

## C1, треугольник

Самая старая тема программы и самая ровная по бумагам: 116 баллов,
39 блоков, восемь приёмов, 52 балла без калькулятора против 54 с ним.
Различает бумаги не сложность, а форма ответа — в Paper 1 просят точное
значение, в Paper 2 три значащие цифры.

### Пятое понятие равенства ответов

Серия снова упёрлась в вопрос «когда два ответа одинаковы», и снова
ответ новый. A3 сверял **значения**, A4 — **форму записи**, A8 —
**множества**, B1 — **уравнения**. Здесь ответ это **конфигурация**:
длина стороны сама по себе ничего не значит, значение имеет треугольник,
частью которого она является.

Отсюда `solve_triangle`: по трём известным частям он достраивает
треугольник целиком — SSS, SAS, ASA, AAS и SSA, — а `verify_triangle`
не хранит эталона вовсе. Он берёт данные условия, строит по ним
треугольник и смотрит, согласуется ли с ним ваш ответ.

Разница с `check_num` видна на одной строке. Хеш от `16.3` пройдёт
и в том случае, когда это число взято из другого треугольника;
`verify_triangle` при `A = 100°` вместо `116°` то же самое число
отвергнет, потому что треугольник с такими частями не замыкается.

### Данных бывает мало для одного ответа

Второе следствие того же взгляда — неоднозначный случай. Две стороны
и угол против меньшей из них задают **два** разных треугольника, и оба
настоящие: `solve_triangle` возвращает список из двух элементов.

Это первый раз в серии, когда неполнота ответа не ошибка ученика,
а свойство данных. В A8 лишний кусок множества был дефектом, в B1
лишний корень появлялся из неравносильного шага. Здесь оба треугольника
законны, и выбирает между ними условие: «smallest possible perimeter».

Поэтому `verify_triangle` в таком случае печатает предупреждение и
пропускает ответ, а правильность выбора ловит уже `check_num`
на периметр. Это честный предел проверки, и он записан в карточку:
две проверки разного типа на один ответ дешевле, чем одна
универсальная, — тот же вывод, что и в B1 с касательными.

### Точное значение как требование к записи

Третья новая проверка — `verify_exact`, и она продолжает линию
`verify_factored` (A4), `verify_nonneg_form` (A8) и `verify_vertex_form`
(B1). «Find the exact value» и «give your answer in the form $p\sqrt q$»
это требования к записи: $29{,}4$ вместо $12\sqrt6$ балла не получает,
и проверка ведёт себя так же — десятичное число она отвергает, даже
если оно совпадает во всех печатаемых знаках.

Половина темы этим и живёт. В Paper 1 дают `cos C = 3/4` или
`cos A = 1/5`, и синус берётся из тождества. Знак снимается сам: угол
треугольника лежит между $0^\circ$ и $180^\circ$, а там синус
положителен. Это единственное место школьной программы, где из
$\sin^2 = k$ следует единственный ответ без разбора случаев, — и в C3
всё будет ровно наоборот.

### Невозможная пифагорова тройка

Главная находка разбора. У блока `2022-NOV-COMMON-P1-Q03-B` в корпусе
записано, что стороны прямоугольного треугольника равны
`a`, `(a²−1)/(2a)` и `(a²+1)/(2a)`.

Такого треугольника не бывает. Невязка теоремы Пифагора для этой
тройки равна `a² − 1`, то есть ноль только при `a = 1`, а в условии
`a > 1`. В бумаге деление на **2**, а не на `2a`, и первый пункт того же
вопроса просит это прямо доказать: «show that a² + ((a²−1)/2)² =
((a²+1)/2)²».

Цена ошибки — множитель `a` в ответе: площадь равна `a(a²−1)/4`,
а не `(a²−1)/4`. Похоже на потерю при извлечении формулы из PDF:
двойка под дробной чертой прочиталась вместе с идущей следом буквой.

Заодно нашлось, что это за тройка: при `a = 3` она даёт `(3,4,5)`,
при `a = 5` — `(5,12,13)`. Те же два треугольника, что в B1 в задаче
про площадь, равную периметру, — и там они появились совсем с другой
стороны.

### Та же бумага, посчитанная дважды

Дублирование ноябрьской Paper 3 2023 года, найденное в B1, всплыло
здесь снова и с другой стороны. В тему треугольника из обеих копий
попал один и тот же блок `P3-Q02-A-II` — перпендикулярные угловые
коэффициенты, один балл. То есть 116 баллов темы включают один
лишний.

Ценно в этом то, что находка воспроизвелась независимо: в B1 удвоились
шесть баллов вопроса 2(c), здесь — один балл вопроса 2(a)(ii). Дубль
задевает каждую тему, куда попали блоки этой бумаги.

### Четырнадцать баллов не про треугольник

Отдельно от дубля: шесть блоков (14 баллов из 116) в теме
`geometry.trigonometry` к треугольнику отношения не имеют вовсе.

Это касательные к семейству решений дифференциального уравнения
(6 баллов), приближение π через интеграл (4), уравнение нормали (1),
два блока про перпендикулярные угловые коэффициенты (2) и формула
расстояния между точками (1). Признак простой и проверяемый машиной:
ни в одном из шести нет ни одного тега `sine_rule`, `cosine_rule`
или `area_of_triangle`.

Причина, похоже, в слове «geometry» в названии темы: классификатор
складывает туда всё, что выглядит геометрией на плоскости, включая
аналитическую. Это не та же болезнь, что с системами линейных уравнений
(там подтемы не существует вовсе), а обратная: подтемы есть, но имя
темы шире, чем её содержание.

### Иллюзия калькулятора наоборот

Во всех прежних практикумах доля «с калькулятором» оказывалась
завышенной. Здесь впервые не так: 55% формально, и они честные.
В Paper 2 треугольники действительно считают численно — теорема
косинусов с углом 116°, направления, высота здания.

Зато обнаружилась деталь того же рода, что полюс в B1. У задачи про
поливалку из архива угол `ADS` обращается в ноль при `t = 6,0255`,
и там же обращается в ноль знаменатель модели `d(t)`. В B1 эта модель
уже встречалась — там численный поиск принимал полюс за корень.
Теперь видно, откуда он берётся: треугольника в этот момент просто
больше нет. Полезное правило: у модели, построенной на треугольнике,
разрывы стоят там, где треугольник вырождается.

### Что ещё стоит помнить про эту тему

Четвёртая находка мелкая, но показательная. В описании блока
`2021-NOV-COMMON-P2-Q02` (неоднозначный случай) сказано «selecting the
acute case that minimises the perimeter». Наименьший периметр даёт
треугольник, у которого угол `B` **тупой** (133,6°), а острым в нём
оказывается угол `C` (21,4°). Формулировка верна только если относить
её к `C`, и в практикуме выбор описан через длину стороны `AB`,
без ссылки на остроту угла.

## B2, композиция и обратные функции

Первый практикум серии на английском: 113 баллов, 40 блоков, девять
приёмов. Обе идеи темы записываются одной строкой алгебры, и ни за одну
из них экзамен не платит. Платит он ровно в двух местах — за **порядок**
композиции и за **область**, на которой обратной разрешено существовать.

### Ноутбук на английском, документация на русском

Ученик сдаёт работу по-английски, и десять русских практикумов ему
в этом не помощники. Но `kit.py` один на всех, и одна забытая строка
означала бы «не сходится» посреди английской работы.

Переключатель добавлен так, чтобы ничего готового не задеть:

```python
_LANG = 'ru'

def language(code=None):
    """Язык сообщений: 'ru' (по умолчанию) или 'en'."""

def _t(ru, en):
    """Один и тот же кусок сообщения на двух языках."""
```

Значение по умолчанию русское, поэтому десять практикумов, тренажёр
и сервер не меняются вовсе; язык просит сам ноутбук вызовом
`language('en')` в установочной ячейке. Все 111 печатающих строк `kit.py`
переписаны через `_t`.

Сторожит это `practicum/tests/check_language.py`, и сторожит с двух
сторон. Статический проход по `ast` находит любую кириллическую строку,
не убранную в русскую половину `_t`, — включая текст, который попадает
в `print` через возвращённое примечание. Динамический прогоняет 104
вызова, покрывающих каждую печатающую функцию в обеих ветках, в режиме
`en` и требует латиницы.

Английский стал и языком карточки приёмов
(`practicum/skills/functions-composition-inverse.yaml`): её `trigger`,
`chain` и `traps` переезжают в теорию ноутбука почти дословно, и держать
их на другом языке значило бы переводить дважды. Карта, `PRACTICUM.md`,
тренажёр и тексты тестов остаются русскими — это документация
репозитория, а не материал ученика.

### Шестое понятие равенства ответов

Серия снова упёрлась в «когда два ответа одинаковы». A3 сверял
**значения**, A4 — **форму записи**, A8 — **множества**, B1 —
**уравнения**, C1 — **конфигурацию**. Здесь ответом служит **функция**,
и верна она тогда, когда **отменяет исходную**.

Поэтому `verify_inverse` не хранит эталона вовсе: он подставляет `f`
внутрь того, что вы написали, и требует `x` обратно на области из
условия.

Направление выбрано одно и намеренно:

```
f(g(x)) = x   пропускает неверную ветвь корня
g(f(t)) = t   её ловит
```

У `f(x) = √(x²−1)` на `[1, 2]` ответ `−√(x²+1)` первую проверку проходит
— под квадратом знак исчезает, — а вторую заваливает, возвращая `−t`.
И это не выдуманный случай: выбор ветви и есть тот R1, за который в
markscheme стоит отдельный балл.

Рядом всегда стоит `check_domain`: область или множество значений
сверяется как множество, поэтому `Interval(0, sqrt(3))`,
`0 <= x <= sqrt(3)` и `[0, 1.73]` равноправны, а `(0, √3)` — уже другой
ответ. Так и в markscheme: запись свободна, концы нет.

### Найденная по дороге ошибка в проверке

Динамический проход `check_language.py` требует от каждой функции не
только латиницы, но и вообще какого-нибудь вердикта. На этом всплыло,
что `verify_root_transform` при многочлене с кратным корнем в нуле
не печатал ничего, а бросал `mpmath.NoConvergence`: `sp.Poly(x**2)
.nroots(n=20)` до сходимости не доходит.

Ученик A4 с неудачным ответом получал не ❌, а упавшую ячейку. Лечится
понижением точности:

```python
def _nroots(expr, var):
    for precision in (20, 15, 10):
        try:
            return sp.Poly(expr, var).nroots(n=precision)
        ...
```

### Лестница в две стороны

Приёмы 1–3 идут вперёд: вычислить композицию, записать её, приравнять
к числу. Приём 4 — самая маленькая композиция, `f(−x)`. Приём 5
составляет `f` с самой собой, пока не станет виден закон.

Приёмы 6–9 идут назад: определяющее свойство `f⁻¹(a) = b ⟺ f(b) = a`,
замена `x` и `y`, область существования, выбор ветви.

Петля закрывается в конце. Функция самообратна ⟺ `f∘f` тождественна ⟺
график симметричен относительно `y = x`. Задание 13 находит такую
функцию алгеброй (`g = g⁻¹` при `a = −3`), а задание на таймере
встречает тот же факт как случай `m = −1` в исследовании Paper 3.

### Пять расхождений с корпусом, и все одного вида

Тема оказалась самой грязной в архиве, и грязь однородная: при
извлечении из PDF теряется дробная черта или знак корня. Ответы
обратных функций — как раз вложенные корни над дробями, так что
попадает по ним.

1. **Ноябрьская сессия 2023 года удвоена целиком.** Прошлые практикумы
   (B1, C1) отмечали дубль только для Paper 3. Проверено построчно: у
   Paper 1 тексты вопросников совпадают посимвольно после вычёркивания
   кода бумаги (`8823‑7106` против `8823‑7111`), у Paper 2 — по буквам
   и цифрам, а расходятся только скобки векторных столбцов, которые одно
   извлечение теряет, а другое отдаёт кусками служебных глифов. Своей
   Paper 3 у зон нет вовсе: файл один, в каталоге `Common`. В B2 удвоены
   пять баллов.
2. **`2022-MAY-TZ2-P1-Q11-B`.** В корпусе `(1+√(x²+4x))/x`, в бумаге
   `1 + √(4x²+x)/x`. Проверить легко: `g(4) = 1/5`, значит `g⁻¹(1/5)`
   обязана вернуть 4. Бумага возвращает 4, корпус — 9,58.
   `verify_inverse` отвергает корпусную запись сам, без эталона.
3. **`2022-MAY-TZ1-P2-Q10-B`.** В корпусе `f⁻¹(x) = 2/x²+1` и сама `f`
   записана неверно. В бумаге `f(x) = √(x²−1)` на `[1, 2]`,
   `f⁻¹(x) = √(x²+1)`. Подтверждается дважды: снимком страницы и
   пунктом (c) той же задачи, где объём вращения равен `π(h³/3 + h)`, а
   это в точности `π∫₀ʰ (x²+1) dx`.
4. **`2021-MAY-TZ2-P2-Q12`.** Функция записана вверх ногами:
   `arcsin((1−x²)/(1+x²))` вместо `arcsin((x²−1)/(x²+1))`. С корпусной
   версией напечатанные в соседних пунктах результаты становятся ложью —
   горизонтальная асимптота выходит `y = −π/2` вместо `π/2`, и функция
   не убывает, а возрастает при `x < 0`. Третий свидетель — markscheme:
   он даёт `y² = (1+sin x)/(1−sin x)`, что получается только из записи
   бумаги.
5. **`2021-NOV-COMMON-P1-Q02-D`.** Единственный случай, где вместе
   с формулой уехал и **ответ**. В корпусе `g(x) = (ax+4)/(x−3)`,
   в бумаге `(ax+4)/(3−x)`. Дробно-линейная функция самообратна, когда
   сумма угловых коэффициентов равна нулю: у бумаги это `a + 3 = 0`,
   то есть `a = −3` (ответ markscheme), у корпуса — `a − 3 = 0`, то есть
   `a = 3`. Что напечатано, показывает и само решение markscheme: в его
   METHOD 2 стоит константа 5, а `5 = 9 − 4` берётся из `3(3−x) − 4`.

Практический вывод для темы: **всякую формулу с корнем или дробной
чертой перечитывать по PDF, а ответ выводить заново, а не списывать.**
Всё перечисленное перепроверено в `practicum/tests/verify_b2.py`
(82 проверки).

### Иллюзия калькулятора, пятый раз

Формально 50 баллов из 113 — с калькулятором, 44%. Настоящей работы для
GDC здесь на **один балл**: `f⁻¹(2) = 1.32` в мае 2025 TZ1 Paper 2,
где `f(x) = 4 ctg x + sin x` и обратной формулы не существует.

Остальные «калькуляторные» баллы — вопросы Paper 2 и Paper 3, которые
просят точную обратную, доказательство чётности или закон для `fⁿ`.
Машина в них не участвует, а карта об этом не знает, потому что делит
не по работе, а по бумаге.

### Где проверка мягче экзамена

* `verify_inverse` ничего не знает про область самой обратной. Верная
  формула с неверно названной областью проходит, и ловит это только
  стоящий рядом `check_domain`.
* `verify_identity` сверяет выражения, а не рассуждение. На «show that
  f is even» ответом принимается сам `f(x)`: подстановка `−x` нигде не
  написана, а балл там R1, за фразу.
* Порядок композиции проверкой не различается. Неверный порядок даёт
  другое выражение, и ученик получает «не сходится» без слова о том,
  в чём дело. За это отвечает тренажёр распознавания.
* `check_series` подставляет числа во все буквы, кроме переменной, по
  алфавиту, поэтому единственную букву можно переименовать незаметно:
  `(x−3)² + p²` пройдёт там, где условие называет постоянную `k`.

## B3, преобразования графиков

### Тема взята целиком, и по-другому нельзя

37 блоков, 107 баллов — вся `functions.graphing`. Соседи заняты:
`functions.equations` разобрали B1 и B2, а `curve_sketching` вместе
с `asymptotes` дают 147 баллов, то есть весь B4 без остатка. Оставлять что-то
на потом было некуда, и `leftovers` в карточке впервые пустой.

Шов между B3 и B4 проходит поэтому не по темам, а по тому, **какую кривую
рисуют**. В B3 — знакомую по форме: парабола, `arccos`, логарифм, синусоида,
прямоугольная гипербола. Про такую надо знать, где она пересекает оси и где
поворачивает, и всё. В B4 — незнакомую, которую сначала исследуют
производными. Формулировка задания это различает буквально: «sketch the graph
of y = arccos x» против «investigate the behaviour of».

### Седьмое понятие равенства ответов

Ответом здесь служит картинка, и это первая тема серии, где ответ вообще
нельзя записать числом. Спрашивают её двумя способами, поэтому и проверок две.

**«Describe a sequence of transformations» — ответ это рецепт.**
`verify_transform` ничего не сравнивает с эталонным описанием: он берёт ваши
шаги и выполняет их над исходной функцией. Отсюда два следствия, и оба верные.
Любое описание, которое действительно приводит к цели, принимается — в том
числе не то, что в markscheme: для `arctan(2x+1)` годится и «сжатие в ½, потом
½ влево», и «1 влево, потом сжатие в ½». А описание, которое не приводит,
отвергается, и это ровно тот балл, который экзамен снимает формулировкой
*«Award A1A0 for correct horizontal transformations specified in the wrong
order»*. Проверка ещё и называет, что именно не так: перепутанный порядок,
растяжение, взятое обратным, сдвиг не в ту сторону.

**«Sketch the graph» — ответ это список особенностей.** Никакой эскиз не
оценивают за красоту. Инструкции в архиве всегда одни и те же — *«stating the
values of any axes intercepts»*, *«clearly indicating any asymptotes»*,
*«showing the coordinates of any points where f'(x) = 0»*, — и эти фразы и есть
markscheme. `verify_sketch` считает каждую названную особенность из самой
функции и сверяет список в обе стороны.

### Излом это не минимум, и оба раза это отдельный балл

Самая содержательная часть новой проверки — различение гладкой вершины
и излома. У `|f|` в каждом нуле `f` нечётной кратности кривая приходит с одним
наклоном и уходит с противоположным, и markscheme просит эти точки по имени:
*«sharp points (cusps) at the x-intercepts»*. Проверка их разводит: у гладкой
вершины односторонние наклоны стремятся к нулю, у излома — нет. Если излом
назвать минимумом, сообщение так и скажет.

Попутно выяснилось, что такая точка принадлежит **двум спискам сразу**: излом
у `|f|` — это одновременно пересечение с осью. И ноль, до которого график
только дотрагивается (кратный корень, или ноль ровно на конце отрезка), скан
смены знака не ловит вовсе. Обе дыры закрыты, и обе описаны в
`test_kit_graph.py`.

### Картинку пришлось превратить обратно в функцию

Шесть заданий архива дают график, а не формулу. Проверить такое нельзя, пока
у графика нет уравнения, поэтому каждое было восстановлено.

Иногда это арифметика: ломаная из мая 2021 TZ1 — отрезок на уровне 4 и парабола
с вершиной (2, 6), проходящая через (0, 4), то есть `6 − (x−2)²/2`. Иногда
интереснее: у мая 2025 TZ3 напечатаны наклонная асимптота `y = 5x + 5`,
вертикальная `x = −½` и две отмеченные точки, и этих четырёх фактов ровно
хватает, чтобы получить `f(x) = 5x + 5 + 5/(4x+2)` — с проверкой, что обе
точки воспроизводятся.

Там, где восстановление меняет условие, ноутбук говорит об этом прямо.
В задании 12 модель нужна только на `x ≥ 0`, потому что `g(x) = |f(|x|)|`
левую половину и не смотрит.

### Чего проверка не видит — ноутбук рисует

Ни `verify_transform`, ни `verify_sketch` не смотрят на форму кривой между
особенностями. Выпуклость, монотонность и «asymptotic behaviour» в списке точек
следа не оставляют, и две разные функции с одинаковым списком получают
одинаковый вердикт — это измерено в тестах.

Поэтому каждое задание на эскиз печатает рядом настоящий график, matplotlib'ом.
Не вместо проверки, а вместо умолчания: посмотреть на него надо после того, как
свой эскиз нарисован, и разница между ними — это ровно тот балл A1 за форму,
который машина присудить не может.

### Первая за шесть практикумов честная доля калькулятора

43 балла из 107 формально с калькулятором, и на этот раз почти все они его
заслуживают. Приём 9 — исследование семейства графиков в Paper 3 — без GDC
не делается по замыслу: «use your graphic display calculator to explore the
graph of y = fₙ(x) for the odd values n = 3 and n = 5» это инструкция, а не
украшение. Координаты в мае 2023 TZ1 Paper 2 тоже читаются с экрана. Пять
предыдущих тем подряд давали калькуляторную иллюзию; эта — нет.

### Шесть расхождений с корпусом

Все перепроверены в `practicum/tests/verify_b3.py`.

**Ноябрь 2025 TZ1 Q8(b)** — вместо целевой функции записана дробь
`(8sin³θ + 6sinθ)/(6sinθ)`, которой в бумаге нет. Она сокращается до
`1 + (4/3)sin²θ`, у неё период π, и никакими растяжениями со сдвигами из
`sin θ` она не получается. В бумаге стоит
`6sin(θ+π/6) − 8sin³(θ+π/6) = 2sin(3θ+π/2)`. Любопытно, что записанный
**ответ** — растяжение 2, сжатие ⅓, сдвиг π/6 влево — для настоящего вопроса
верен. Ошибку нашла попытка проверить ответ, а не прочитать условие.

**Май 2025 TZ3 Q6** — у обеих отмеченных точек перепутаны знаки: в корпусе
A(−½, 5/2) и B(0, −15/2), в бумаге A(−1, −5/2) и B(0, 15/2). Корпусная версия
внутренне невозможна: она ставит локальный максимум ровно туда, где
вертикальная асимптота.

**Май 2025 TZ3 Q6(b)** — в записанном методе вертикальная асимптота `x = −½`
«остаётся» асимптотой `y = 15/f(x)`. Не остаётся: у `f` вещественных нулей нет
(дискриминант −300), значит у `15/f` нет ни одной вертикальной асимптоты,
и markscheme её не просит. Это ровно та половинчатая замена, ради которой
приём и существует.

**Ноябрь 2021 Q10(b)** — конец отрезка записан как (3, −5), а `v(3) = −11`.
Остальное в разборе верно.

**Ноябрь 2022 Q10** — у функции потерялись показатели степени: записано
`cos2x − 3sin2x`, в бумаге `cos²x − 3sin²x`. Спор разрешает сам markscheme —
он сводит уравнение к `cos2x = ½` и печатает корни π/6 и 5π/6, а это корни
именно версии с квадратами; у записанной вышли бы π/12 и 7π/12.

**Ноябрь 2023 P2 Q02(b)** — ноябрьский дубль, найденный в B2, добрался
до второй темы: два балла из 107 посчитаны дважды.

Пять из шести — один вид: при извлечении из PDF теряется показатель, знак или
цифра. B2 сформулировал это правило про дробную черту и корень; B3 добавляет
к списку показатель степени.

## B4, исследование функции, асимптоты, эскиз

### Две темы, взятые целиком, потому что в архиве они одна

62 блока, 147 баллов: вся `functions.curve_sketching` (113) и вся
`functions.asymptotes` (34). По отдельности ни одна не дотягивает до нижней
границы в 60 баллов, а вместе они и в бумаге стоят одним вопросом. Ноябрь 2021
Paper 2 Q10: пересечения с осями, вертикальная асимптота, наклонная асимптота,
эскиз — четыре части подряд, 11 баллов из 18, и разрезать их по темам значит
разрезать вопрос. То же в ноябре 2023 Q11 и в мае 2021 TZ1 Q11.

Так что `leftovers` пуст второй раз подряд.

Шов с B3 при этом оказался виден в цифрах. У B3 на Paper 1 приходится 64 балла
из 107 и калькулятор весит 40%; здесь Paper 1 даёт 39 из 147, а калькулятор —
73%. Темы выглядят одинаково и печатаются на **разных бумагах**, и это следствие
шва, а не совпадение: B3 рисует кривые, форму которых полагается знать, поэтому
экзаменатор может спросить их без машинки на столе. B4 рисует кривые, которых
никто не рисовал, и найти вручную точку поворота у `e^x − 3x − 4` — другой
вопрос, чем нарисовать её.

### Восьмое понятие равенства ответов: ответ это прямая

A3 сверял значения, A4 — форму записи, A8 — множества, B1 — уравнения,
C1 — конфигурацию, B2 — функцию по тому, что она отменяет, B3 — картинку
по списку особенностей. Здесь ответом служит **прямая**, и верна она не тогда,
когда совпала с эталоном, а тогда, когда кривая к ней действительно
приближается.

Поэтому `verify_asymptotes` не сравнивает записи, а считает предел. Вертикальная
`x = c` — односторонний предел в `c` бесконечен. Горизонтальная `y = h` — предел
на бесконечности равен `h`. Наклонная `y = mx + b` — предел разности
`f(x) − (mx + b)` равен нулю. Это буквально определение асимптоты, и больше
ничего не нужно: три способа markscheme (деление уголком, приравнивание
коэффициентов, `lim f(x)/x`) дают одну и ту же прямую и проходят одинаково.

Отсюда же требование записать ответ уравнением. `x = 3` — прямая, `3` — число,
и markscheme говорит об этом прямым текстом: *«must be written as an equation
with y ='*, *«Do not award the final A1 if the answer is not given as an
equation»*. Проверка принимает любую запись, задающую прямую, — в том числе
`Eq(2*x + 6, 0)`, которое markscheme тоже принимает, — и отвергает ту, что
задаёт число.

Полнота проверяется отдельно от правильности: пропущенная асимптота и лишняя
дают разные сообщения, потому что в markscheme это разные баллы. Самая частая
ошибка темы ловится именно так: у `(4x² − 1)/(3x + 2)` числитель старше
знаменателя, горизонтальной асимптоты нет вовсе, а отношение старших
коэффициентов `y = 4/3` — правдоподобный неверный ответ. Проверка отвергает его
не по списку, а по определению: `g(x) − 4/3` не стремится к нулю, оно стремится
к бесконечности.

### Множество значений: один символ, и в нём весь балл

Рядом стоит `verify_range`, и устроен он как `verify_param_set` из A8 — ничего
не решает за ученика, а спрашивает у самой функции. Внутри вашего множества
значение должно достигаться, снаружи — не должно.

Главное в нём — что каждый конец проверяется отдельно, вместе с соседями
на расстоянии `eps`. Там и стоит разница между `≤` и `<`: точка поворота
достигается и получает нестрогое неравенство, горизонтальная асимптота
не достигается и получает строгое. Markscheme ноября 2023 Q11(e) пишет это
без обиняков — *«Award A1A0 for strict inequalities in both»*.

Соседи на расстоянии `eps` понадобились после первой же попытки. Точки берутся
внутри промежутков ответа и его дополнения, и на неограниченном куске
`(−∞, −4)` они уходят далеко влево. Ответ `y ≥ −4` при истинном `y ≥ −5`
проходил: полоска между −5 и −4 не содержала ни одной пробы. Теперь каждая
граница проверяется трижды — сама и по шагу в обе стороны, — и сдвинутая
граница ловится.

### Три задания, где список особенностей читают боком

Пятый приём — множество значений — это тот же рисунок, прочитанный по
вертикальной оси вместо горизонтальной, и в практикуме он стоит между эскизами
не случайно. У `(x² − 14x + 24)/(2x + 6)` максимум лежит **ниже** минимума
(−18.7 против −1.34), и это возможно только потому, что между ними проходит
вертикальная асимптота. Отсюда разрыв в множестве значений — и отсюда же,
задом наперёд, понятно, зачем эскиз рисовали.

Экзамен даёт два маршрута и платит за оба одинаково: точки поворота
с калькулятора (−18.7 и −1.34) или дискриминант `y = f(x)`, разрешённого
относительно `x`. Второй даёт точный ответ `−10 ± 5√3`, и следующая часть
бумаги его требует. То же у мая 2023 TZ2: дискриминант `12y² − 20y + 4` даёт
`(5 ± √13)/6`, калькулятор — `0.232` и `1.43`.

### Кривая, которая не функция

Восьмой приём — `y² = f(x)`. Проверять его нечем: `verify_sketch` берёт
функцию, а здесь функции нет. Поэтому в ноутбуке спрашиваются особенности
**верхней ветви**, `y = √(f(x))`, и отдельно то, чем ветви различаются.

Различие оказалось единственным числом. У `y² = x³` производная верхней ветви
`(3/2)√x` в нуле обращается в ноль: обе ветви приходят к оси плоско и образуют
излом. У `y² = x³ + 1` в точке `x = −1` производная бесконечна: ветви приходят
вертикально и смыкаются в гладкую кривую. Это и есть ответ на вопрос бумаги
*«identify two key features that would distinguish one curve from the other»*,
и он проверяется двумя пределами, а не описанием.

### Форма кривой: приём, который проверить нельзя, и что сделано вместо

`verify_sketch` не смотрит на кривую между особенностями — это установлено
ещё в B3. Здесь ограничение бьёт сильнее: седьмой приём **целиком** про форму
между ними. *«Sketch the curve, taking into consideration the relative values
of the second derivative found in part (b)»* — и слово «relative» тут вся
задача. Знаки `f''` дают вид точек, это предыдущая часть; размеры дают форму.

Поэтому приём проверяется не картинкой, а самой второй производной. У
`e^{cos 2x}` в максимумах `f'' = −4e ≈ −10.9`, в минимуме `4/e ≈ 1.47`,
и отношение ровно `e² ≈ 7.4`. Ноутбук спрашивает оба значения и отношение —
три числа, за которыми стоит ровно то, что рисуют: два острых пика и длинное
плоское дно.

Все остальные задания на эскиз печатают рядом настоящий график. Смотреть
на него — после того, как нарисовали свой.

### Семейство проверяется без единого эталона

Последнее задание, на таймере, — исследование `y = x³ + ax² + b` из ноября
2023. Шесть его частей спрашивают множества значений `b`, и ни одно из них
не записано в проверке. Вместо эталона написана сама постановка вопроса:

```python
def crossings(a_value, count):
    def holds(b_value):
        roots = Poly(x**3 + a_value*x**2 + b_value, x).real_roots()
        return len(set(roots)) == count
    return holds
```

`verify_param_set` из A8 берёт ваше множество и спрашивает у этой функции
в точках: внутри — выполняется ли, снаружи — не выполняется ли, и отдельно
на каждой границе и по обе стороны от неё. Границы здесь и есть содержание
вопроса: `b = −4` и `b = 0` дают ровно два пересечения и не принадлежат
ни одному из открытых промежутков ответа. Написать `≤` — стандартная потеря
балла, и она ловится.

### Что нового понадобилось от kit

Две проверки, `verify_asymptotes` и `verify_range`, обе описаны выше.
Ничего больше: эскизы делает `verify_sketch` из B3, множества значений
опираются на `_as_set` и `_pieces` из A8, семейство — на `verify_param_set`
оттуда же. `test_kit_curve.py` меряет обе новые проверки на функциях самого
архива: 47 утверждений, включая то, что они отвергают и чего не видят.

### Шесть расхождений с корпусом

**Май 2021 TZ1 Q11(e)** — функция записана как `(4x²+3)/(2−3x)`, в бумаге
`(4x²−1)/(3x+2)`. Markscheme печатает `x = −2/3` и `y = 4x/3 − 8/9`; у записанной
версии вертикальная асимптота была бы в `+2/3`, а наклон отрицательным. Условие
`x ≠ −2/3` в самой бумаге закрывает вопрос о знаменателе.

**Май 2023 TZ2 Q8(a)** — записано `(2−5x²)/(3−x²)`, в бумаге `(2x−5)/(x²−3)`.
Разрешает спор дискриминант: markscheme пишет `12y² − 20y + 4`, и это
дискриминант именно версии бумаги. У записанной вышел бы `12y² − 68y + 40`,
и никакого `√13` в ответе не появилось бы.

**Май 2025 TZ2 P3 Q1(a)** — записано `(x²−16)/(x²+16)`, в бумаге
`x(x²−16)/(x²+16)`. Корпус спорит сам с собой: соседний блок записывает
пересечения `(−4,0)`, `(0,0)`, `(4,0)`, а записанная функция в нуле равна −1.
Кроме того, у неё есть горизонтальная асимптота и нет наклонной, а часть
(d)(ii) той же задачи требует именно наклонную.

**Ноябрь 2023, дубль** — сессия лежит в корпусе двумя зонами, а бумага одна.
Установлено постранично ещё в B2; B3 нашёл два балла, здесь удвоено восемь
блоков и 20 баллов из 147. Настоящего материала около 127.

**Ноябрь 2023, две копии не согласны между собой** — и это уже новое. Диапазон
`f` в Paper 2 Q11(e) записан только под TZ2. Из восьми частей исследования
Paper 3 копия TZ1 записывает семь, копия TZ2 — две, и общая у них одна. Обе
копии — одна и та же бумага, значит истина это объединение, а не любая из них
по отдельности. Читай практикум только копию TZ1, пятый приём потерял бы
четыре балла, а девятый — один.

**Ноябрь 2025 TZ3 Q9** — функции в корпусе нет вовсе, а печатный текст
схлопывается в `f(x) = 1 2 13 2 x kx`. Восстанавливает её markscheme: он считает
дискриминант как `k² − 26`, а это требует старшего коэффициента ½, и он же
даёт ось симметрии `x = −5` при `k = 5`, что бывает только при нём. Значит
`f(x) = x²/2 + kx + 13`.

Три из шести — снова один вид: при извлечении из PDF теряется дробная черта,
множитель или показатель. B2 сформулировал правило про черту и корень, B3
добавил показатель, B4 добавляет ведущий множитель.

## B5, показательные и логарифмические модели

[`practicum/functions/practicum-b5-exponential-log-models.ipynb`](practicum/functions/practicum-b5-exponential-log-models.ipynb)
— 43 ячейки, 85 баллов архива, [карточки приёмов](practicum/skills/functions-exponential-log-models.yaml).
Четвёртый практикум серии на английском, устроенный как B2–B4.

### Тема в две сентенции

Вся тема — это два предложения, и весь практикум построен на том, чтобы ученик
за секунду понимал, какое из них перед ним.

> `50 = 100e^{−5730k}` — **неизвестное в показателе.** Единственный ход вниз —
> логарифм обеих частей.
>
> `3 log₈ 10x − log₄ x = 1` — **неизвестное под логарифмом.** Единственный ход
> вверх — возведение основания в степень.

Всё остальное — обстановка вокруг. Модель населения это первое предложение
с двумя точками данных вместо одной. Сложный процент — оно же с основанием,
записанным как `1 + r`. Децибелы — второе предложение с множителем 10 впереди.

Отсюда и лестница: приёмы 1–3 поднимают неизвестное из-под логарифма (три
закона, смена основания, уравнение), приёмы 4–6 опускают его из показателя
(законы степеней, логарифмирование, подгонка модели), приёмы 7–8 — две модели,
которые архив действительно спрашивает, а девятый стоит сбоку: логарифм не как
инструмент, а как функция.

### Четыре подтемы, потому что ни одна не дотягивает

| подтема | блоков | баллов |
|---|---|---|
| `functions.logarithmic_functions` | 13 | 35 |
| `functions.mathematical_models` | 13 | 20 |
| `functions.exponential_models` | 6 | 18 |
| `number_algebra.exponential_models` | 5 | 12 |

85 баллов при нижней границе диапазона 60 — самая тонкая из готовых тем. Взять
пришлось все четыре целиком: наибольшая даёт 35 баллов, то есть вдвое меньше
границы. И это не натяжка: архив их тоже не разделяет. Ноябрь 2025 Paper 3
подгоняет логистическую кривую, логарифмирует её и решает стоящее за ней
дифференциальное уравнение одним вопросом на 29 баллов.

Заодно это первый случай, когда практикум переходит границу разделов: тема
`number_algebra.exponential_models` лежит в разделе «Число и алгебра», а
практикум — в «Функциях». Приём там тот же самый (процент как множитель),
и в бумаге проценты стоят рядом с e-моделями.

### Девятое понятие равенства ответов: ответ это модель

Серия шла так: A3 сверял значения, A4 — форму записи, A8 — множества, B1 —
само уравнение, B2 — функцию через обратную, B3 — рецепт преобразований,
B4 — прямую через предел, C3 — множество корней. Здесь ответом служит **модель**,
и верна она не тогда, когда её постоянные совпали с эталонными, а тогда, когда
она воспроизводит данные, из которых её строили.

`verify_model(label, got, data)` не хранит ни постоянных, ни готового значения.
Он берёт `data` — список пар `(вход, выход)` прямо из условия — и подставляет
каждую в ответ ученика:

```python
verify_model('6.7a', q6_7a, [(0, 15000), (8, 13350)])
```

Побочная выгода важнее прямой. Проверка ловит главную потерю баллов темы,
которую никакая сверка с эталоном не поймала бы: **постоянные найдены верно,
а отсчёт времени сдвинут.** В ноябре 2022 модель начинается 1 января 2014 года,
второй факт про 1 января 2022, а спрашивают про 2041-й — то есть `t = 8` и
`t = 27`. Ответ с `t = 19` безупречен для другого вопроса. Markscheme платит
за это отдельным баллом («recognizing that t = 8 on 1 January 2022»), и
`verify_model` называет ровно ту точку, где модель разошлась с условием,
не выдавая правильного числа.

Вторая новая проверка мельче, но без неё вопрос был бы непроверяем.
`verify_in_terms_of(label, got, want, subs)` сторожит формулировку «выразите
log₁₀ 24 через p и q»: она требует двух вещей сразу — что чужих букв в ответе
нет и что после подстановки истинных значений получается нужное число. Без
первого условия ответ `log₁₀ 24`, переписанный сам через себя, проходил бы,
а он не ответ, а вопрос.

Обе живут в `kit.py` и промерены в
[`practicum/tests/test_kit_model.py`](practicum/tests/test_kit_model.py):
25 утверждений, включая то, что отвергается — сдвинутое время, положительное
`k` при убывании, оставшаяся в модели буква, произведение вместо суммы.

### Четыре хеша из тридцати шести

Доля хешей упала до самой низкой в серии: у C3 их было девять из тридцати семи,
здесь четыре из тридцати шести проверок практикума и архива вместе. Причина
в теме, а не в проверках: она почти всюду даёт либо точную форму, либо модель.

* точная форма встречается чаще, чем ожидалось от «калькуляторной» темы:
  `ln 2 / 5730`, `10^{−1/2}`, `60 + 10 log 2`, `3p + q`, `1/9`, `(1/5)ln(28/13)`.
  Всё это `verify_exact`, и десятичная запись там не принимается;
* модель проверяется подстановкой данных и эталона не требует вовсе;
* хеш остаётся ровно там, где ответ — округление: 2380 лет, 10 100 человек,
  1041 динар, €5906.23.

### Иллюзии калькулятора здесь нет

Пять практикумов подряд формальная доля GDC оказывалась выше настоящей. Здесь
54% баллов с калькулятором, и разрез настоящий: он проходит по бумаге почти
без исключений. Paper 1 спрашивает законы и точную форму, Paper 2 и Paper 3 —
подгонку модели под данные и предсказание. Один и тот же приём «логарифм обеих
частей» даёт `ln 2 / 5730` на одной бумаге и `2380` на другой.

Более того, в этой теме нашлось единственное место во всём разобранном архиве,
где markscheme печатает оба маршрута рядом и платит за них одинаково — май 2021
TZ2 P2 Q5(c):

> **EITHER** using an appropriate graph to attempt to solve for `t` **(M1)**
> **OR** manipulating logs to attempt to solve for `t` **(M1)**

Под это отведён отдельный блок ноутбука «The same question on Paper 2». Смысл
не в том, что калькулятор разрешён, а в том, чего он не даёт: точной формы.
В части (c) это ничего не стоит, в части (b) той же задачи — весь балл.

### Ошибка, которую поймала только сверка с бумагой

Первая версия теории 7 утверждала: «инфляция делит, а не вычитает», и что
вычитание даёт напечатанный в markscheme неверный ответ 164 месяца. Проверка
`verify_b5.py` не сошлась, и чтение markscheme показало, что утверждение
неверно **дважды**.

Во-первых, вычитание — это **METHOD 1** самого markscheme: `r = c − i = 1.5 −
0.8 = 0.7%`, полные баллы. Точное частное `1.015/1.008 = 1.00694` идёт вторым
методом, и оба дают тот же месяц. Во-вторых, 164 месяца получаются совсем из
другого: если принять 0.7% за **годовую** ставку и начислять помесячно,
`ln 1.1 / ln(1 + 0.007/12) = 163.5`. Ошибка не в вычитании, а в периоде.

Заодно нашлось третье: `30000 × 0.85¹⁰` это €5906.23, а не €5906.68, как было
написано по памяти. Хеш при этом был верен с самого начала — он считался
формулой, а не переписывался из прозы, и именно поэтому расхождение вылезло
как провал теста, а не осталось в разборе.

### Три замечания к разметке

**2025-NOV-TZ3-P1-Q08-A** помечен темой `mathematical_models`, но ни
показательной, ни логарифмической функции в нём нет: это «show that
`T = 500 sec θ + (2500 − 1000 tan θ)/3`» для задачи о времени в пути по пляжу.
Приём — выразить длины через угол, место блока в C1 или в оптимизации. В архивный
ноутбук он не вошёл, и это единственный вычтенный блок.

**2023-MAY-TZ1-P2-Q03-A** описывает квадратичную модель `y = −0.6T² + 23T + 110`.
Тема `mathematical_models` объединяет блоки по слову «модель», а не по приёму,
и это видно: из тринадцати её блоков к показательным моделям относятся два.

**Четыре пункта исследования май 2023 TZ1 P3 Q2** — подстановка чисел в уже
доказанную формулу `(S/n)ⁿ`. Настоящая тема того исследования — неравенство
о средних, и она лежит в A8.

## E1, пределы, неопределённости, правило Лопиталя

Пятый практикум серии на английском. Ноутбук, карточка приёмов и сообщения
проверок английские; эта запись, карта и тесты остаются русскими.

### Самая маленькая тема и самая связная

**73 балла, 20 блоков, девять приёмов.** Нижняя граница гранулярности — 60,
так что тема идёт одним практикумом и другого выхода нет: соседи по секции
заняты E2–E9 целиком.

Зато связность здесь выше, чем где бы то ни было в архиве. Одиннадцать блоков
из двадцати — буквально одно правило, применённое от одного до трёх раз.
Вся тема сводится к одной строчке:

> Подставить. Если вышло число — это и есть предел. Если вышло 0/0 или ∞/∞ —
> появилась лицензия дифференцировать, и не раньше.

Порядок этот не педантизм, а место, где лежат баллы. Май 2025 TZ2 даёт пять
баллов за предел, равный нулю, и первый из них — за то, что `(3x+1)e^{-3x}`
переписали как `(3x+1)/e^{3x}`: произведение не дробь, а правило Лопиталя
читает дроби. Схема оценивания говорит это прямым текстом: *«This first A1
must be seen»*.

### Два дубля, и оба разной природы

Корпус числит 22 блока и 81 балл. Два блока дублируют другие.

**Ноябрьский Paper 3 2023 года** физически лежит в Common, а корпус держит его
двумя зональными копиями — `2023-NOV-TZ1-P3-Q02-F` и `2023-NOV-TZ2-P3-Q02-F`.
Отрисованные страницы вопроса и схемы оценивания совпадают побайтово (сверено
md5). Та же беда отмечена в карточках B1 и C1; это дефект разметки.

**Вопрос про `sin²(kx)/x²`** в ноябре 2023 поставлен в TZ1 и TZ2 слово в слово.
Бумаги здесь действительно разные — 8823-7106 и 8823-7111, — и разметка не
виновата: совпадение в самих бумагах. Но для архивного ноутбука разницы нет,
и вопрос входит один раз.

Отсюда 20 блоков и 73 балла вместо 22 и 81.

### Десятое понятие равенства ответов: ответ это предел

Девять предыдущих тем дали девять способов спросить «верен ли ответ». Здесь
появился десятый, и он самый радикальный: **у предела нет числа, которое можно
взять и сравнить.** Предел определён тем, к чему выражение подходит.

Поэтому проверка подходит вместе с ним. `verify_limit` берёт выражение из
условия, строит лестницу точек — `10⁻¹, 10⁻², …, 10⁻⁸` с обеих сторон, или
`10, 10², …, 10⁸` при пределе на бесконечности, — вычисляет выражение в каждой
с запасом в шестьдесят разрядов и спрашивает: садятся ли значения на названное
число?

```python
verify_limit('4b', q4b, atan(2*x)/tan(3*x))
verify_limit('7c', q7c, (cos(x)**n - 1)/x**2, params={n: (1, 2, 5, 9)})
verify_limit('8a', q8a, (exp(b) - b - 1)/exp(b), var=b, point=oo)
```

Эталона не хранится вовсе. Ошибиться вместе с проверкой нельзя, потому что
сверять не с чем.

**Решает самая тонкая ступень, а не средняя по лестнице.** Это выяснилось
не сразу. У дроби `(3x-1)/(2x+1)` на `x = 10⁵` ошибка ещё `10⁻⁵`, и по такой
ступени `3/2` не отличить от `1.49999`. Первая версия усредняла ошибку по
верхней половине лестницы и отвергала верный ответ. Считать надо по ступени
`10⁸`, где ошибка уже `2.5·10⁻⁸`, и отдельным условием требовать, чтобы ошибка
не росла.

**Проверка ловит ровно две потери баллов темы.** Ответ, в котором осталась `x`,
отвергается сразу: предел — число, а не выражение. И число, снятое после
одного применения правила там, где форма была ещё 0/0, просто не сходится к
тому, что написали.

### verify_indeterminate: балл за проверку, а не за вычисление

«Show that the limit is in indeterminate form» стоит в архиве отдельным баллом.
Балл этот — за лицензию: без неё правило Лопиталя неприменимо, а второе его
применение без повторной проверки формы — стандартная потеря.

```python
verify_indeterminate('11a', q11a, TOP, (x - 1)**2, point=1, params={n: (2, 5, 8)})
```

Проверка смотрит на числитель и знаменатель порознь, той же лестницей, и не
верит на слово: заявленное 0/0 там, где на самом деле ∞/∞, не проходит, а на
определённой форме она говорит, куда идёт каждая часть.

### Два хеша из двадцати трёх

Самая низкая доля в серии, и ровно по причине выше. Хеш нужен там, где ответ
нельзя подставить обратно в условие; здесь подставить можно почти всегда.

Остались два, и оба одного рода: ответ **не предел, а постоянная, подобранная
под предел**. `k = 4`, чтобы `lim sin²(kx)/x² = 16`; `k = 9`, чтобы плотность
интегрировалась в единицу. Такое число ничему не удовлетворяет само по себе —
его проверяют по хешу.

### Иллюзия калькулятора здесь почти полная

44% баллов формально с калькулятором. Считать им нечего: ответы темы — `2/3`,
`−3`, `−1/4`, `π/4`, `−n/2`, `n(n+1)/2` — точные всюду, независимо от бумаги.
Единственные два числа, ради которых калькулятор и правда нужен, — те самые
`k = 4` и `k = 9`, и оба не пределы.

Это первый практикум, где разрез «Paper 1 против Paper 2» не работает вообще.
В B5 он был настоящим, в B2 и B3 иллюзорным наполовину; здесь его нет.

### Ошибка, которую поймало только чтение в 300 dpi

Схема оценивания мая 2024 TZ2 печатает вторую производную числителя как

```
16 sec⁴x tan²x + 4 sec⁶x − 2 sin²x + 2 cos²x
```

При отрисовке в 150 dpi я прочитал `+ 2 sin²x − 2 cos²x` — знаки местами. При
такой записи предел выходит `−1`, а не `−3`, и я успел написать в разбор целый
абзац про опечатку в схеме оценивания IB.

Опечатки нет. Перерисовка той же страницы в 300 dpi показала правильный
порядок; `sympy` подтвердил, что напечатанное выражение и есть настоящая вторая
производная. Вывод простой и стоит того, чтобы его записать: **утверждение
о схеме оценивания требует чтения в 300 dpi.** 150 хватает на условие задачи
и не хватает на знак перед слагаемым.

### Три замечания к разметке

Оба дубля выше плюс третье: **2022-NOV-COMMON-P2-Q07-B** вторичными темами
числит `geometry.trigonometry` и `geometry.vectors`. Векторы там есть, но весь
балл пункта (b) — за то, что `1/n → 0`. Разметка `method_family =
limits_and_series` верна; отмечено, чтобы блок не ушёл в C-практикумы при
пересборке карты.

### Лестница идёт по тому, что дала подстановка

| ступень | приём | что её вызывает |
|---|---|---|
| 1 | подстановка и сокращение | точка в области определения, или 0/0 от общего множителя |
| 2 | старшая степень на бесконечности | `x → ∞`, степени и корни |
| 3 | правило Лопиталя, один раз | 0/0 или ∞/∞, сокращать нечего |
| 4 | правило Лопиталя, несколько раз | знаменатель `x³`, `x⁴` |
| 5 | ряд Маклорена вместо правила | `e`, `sin`, `cos`, `ln` около нуля |
| 6 | постоянная под конечный предел | буква в условии, «a finite limit only exists» |
| 7 | предел по параметру | стрелка стоит не под `x` |
| 8 | правило с буквой внутри выражения | `x^{n+2}`, `S_n(x)` |
| 9 | что предел значит | «state a reason», «explain the significance» |

Ступени 6–8 — сюрприз темы. Восемнадцать баллов там, где ответом служит формула
от буквы, а не число, и именно там теряет больше всего тот, кто отрабатывал
одно 0/0.

## E2, ряды Маклорена

Шестой практикум серии на английском и архивный ноутбук к нему.
99 баллов, 26 блоков корпуса, девять приёмов.

### Тема, где разрез по бумагам наконец настоящий

Четыре практикума подряд — B5, B2, B3, E1 — заканчивались одним и тем же
наблюдением: доля баллов с калькулятором ничего не значит, потому что ответы
всё равно точные. В E1 иллюзия была полной: 44% с GDC и ни одного числа,
ради которого он нужен.

E2 — первая тема, где это не так. **43% баллов с калькулятором, и считать
там есть что:** π к четырём знакам, √3 в виде c/d, y(0.15) до шести значащих
цифр, оценки ошибки порядка 10⁻⁵, перебор по числу членов ряда. Разрез
проходит ровно по половине лестницы: приёмы 1–7 строят ряд и живут в Paper 1,
приёмы 8–9 им пользуются и живут в Paper 2 и 3.

Устроена тема необычно концентрированно. **56 баллов из 99 — один вопрос**
в конце Paper 1: двадцатибалльный разбор одной функции от ряда до приближения.
Ещё 21 балл — ноябрьское исследование 2025 года про arctan и π. Два вопроса,
три четверти темы. Когда ряды Маклорена появляются на бумаге, они появляются
двадцатибалльным вопросом, а не пятибалльным.

### Одиннадцатое понятие равенства ответов: ряд определён функцией

Десять предыдущих тем дали десять способов спросить «верен ли ответ». Здесь
появился одиннадцатый, и он ближе всего к определению самого объекта.

Ряд Маклорена до x^k — это **не какой-то многочлен, который надо угадать**,
а единственный многочлен своей степени, прилегающий к функции в нуле теснее
всех прочих: разность f − P обязана обнулиться до x^k включительно. Отсюда
и проверка:

```python
verify_maclaurin('4.1', q4_1, exp(x)*sin(x), order=3)
verify_maclaurin('2.1', q2_1, sin(x**2), terms=2)
verify_maclaurin('1.3', q1_3, cos(x)**n, order=2, params={n: (2, 3, 7)})
```

`verify_maclaurin` берёт функцию из условия, вычитает написанное и смотрит,
с какой степени начинается остаток. Эталона не хранится вовсе. Сообщение при
провале называет **самый низкий разошедшийся член и ничего больше**: «член
с x^2 расходится с функцией; всё, что ниже, сходится». Это точный указатель
и не подсказка.

**`terms=k` берёт глубину из самой функции, а не из ответа.** «Первые два
ненулевых члена» у sin(x²) — это x² и x⁶, у 4x·sin(x²)cos(x²) — x³ и x⁷,
а у sin²(x²) — x⁴ и x⁸. Фиксированной глубины, годной для всех, не
существует, и проверка раскладывает функцию ровно настолько, насколько нужно
именно ей.

**Где проверка мягче экзамена — там же, где и markscheme.** Лишние верные
члены сверх заказанных не отвергаются. Схема оценивания мая 2024 говорит это
прямым текстом: *«condone presence of any additional terms once the first two
correct terms are seen»*.

### Ещё две проверки, и обе из-за того, что эталона нет

**`verify_series_solution`** — для приёма 7, где функции нет вообще: она задана
дифференциальным уравнением. Формулы для неё может не существовать в замкнутом
виде, и сравнивать не с чем даже в принципе. Проверка подставляет многочлен
в само уравнение:

```python
verify_series_solution('7.1(i)', q7_1, (x**2*y - y)/(x**2 + 1), ic=3, order=3)
```

Многочлен P — начало ряда решения до x^k тогда и только тогда, когда P(0)
равно начальному условию и невязка P′ − rhs(P) обнуляется до x^(k−1).
Оценка тугая: ошибись P в члене x^j, и невязка сломается ровно на x^(j−1).
Сообщение поэтому называет тот член, который неверен, а не тот, на котором
сломалась невязка.

**`verify_terms`** — для приёма 9. Ошибка от n членов знакочередующегося ряда
не превосходит члена номер n + 1, и вся потеря баллов приёма — одна и та же
ошибка на единицу. Проверка примеряет границу к обоим соседям:

```python
verify_terms('9.1', q9_1, (1/sqrt(3))**(2*k - 1)/(2*k - 1), 0.0001)
```

Не выполняется на n — «не хватает». Выполняется уже на n − 1 — «больше, чем
нужно; проверьте, к какому члену примеряете границу». Ответа она при этом
не называет.

### Шесть хешей из двадцати трёх, и это не деградация

E1 обошёлся двумя хешами на двадцать три ячейки и записал это как достижение.
E2 требует шести, и причина не в слабости конструкции, а в самой теме.

Хеш нужен там, где ответ **нельзя подставить обратно в условие**. Первая
половина E2 подставляется прекрасно: ряд проверяется прилеганием, решение
уравнения — подстановкой в уравнение, число членов — примеркой границы,
значения m — подстановкой в коэффициент. Вторая половина состоит из
приближений: 61/105, 500/279, π ≈ 3.1412, ошибка 3.7·10⁻⁵. Приближение — это
число, а не соотношение, и подставлять его некуда по определению.

Вывод, который стоит записать: **чем полезнее ответ, тем меньше он проверяем
по существу.** Точная форма проверяется собой, приближение — только сверкой.

### Приближённое равенство нельзя переставлять как уравнение

Самое неожиданное, что нашлось при сборке, — и нашлось не глазами,
а провалившимся тестом. В мае 2024 TZ2 предпоследний пункт даёт

```
(1 - 2x)^(-1/2) (1 - 4x)^(-1/2) ≈ (2 + 6x + 19x^2)/2
```

и просит при x = 1/10 найти приближение для √3. Точная левая часть
равна 10/(4√3), приближённая правая — 279/200. Схема оценивания решает
относительно √3 и получает **500/279 = 1.7921**.

Я написал в разбор, что переставить сначала и получить 5√3/6 ≈ 279/200,
то есть √3 ≈ 837/500 = 1.674, — грубая ошибка. Тест это утверждение
проверял и провалился: **837/500 не хуже, а на волос ближе.** Настоящее
значение 1.7321; одна дробь промахивается на +3.5%, другая на −3.4%.
Они лежат по разные стороны, и обе — один и тот же промах.

Механика простая. Приближение 279/200 занижает точное 10/(4√3) в 0.9665
раза. Если √3 при выделении остаётся в знаменателе, он получает
множитель 1/0.9665; если его перенесли наверх раньше — 0.9665. Уравнение
переживает умножение на √3, а приближённое равенство переносит свою ошибку
вместе с собой, и на какую сторону она ляжет, решает порядок алгебры.

Правило, которое из этого следует, короткое: **оставить искомое там, куда
его поставила точная часть, и выделять в последнюю очередь.** А написанный
абзац пришлось переписать целиком — второй раз за серию тест ловит
не расхождение кода, а неверное утверждение в разборе.

### Шесть замечаний к разметке, и три из них в одном вопросе

Тема побила рекорд по числу расхождений корпуса с бумагой.

**Ноябрь 2025 TZ3, пункты (e), (h), (i)** — во всех трёх подынтегральное
выражение записано как `(arctan x)/x`, а в бумаге стоит `arctan x`, без
деления. Проверяется арифметикой: ∫₀^(1/√3) arctan x dx = π/(6√3) − ½ln(4/3) —
ровно то равенство, которое просят показать, и с делением на x оно неверно.
Ошибка размножилась по трём соседним блокам, потому что классификатор
разбирал их одним проходом.

**Май 2022 TZ1 Q12(b)** — верхний предел интегрирования записан как 1/2,
в бумаге 1. Схема оценивания даёт 61/105 = 1/3 + 1/5 + 1/21, то есть
значение именно на [0, 1].

**Май 2024 TZ2 Q12(c)** — корпус приводит ответ к виду 1 + 3x + 19x²/2,
бумага просит показать (2 + 6x + 19x²)/2. Числа те же, но «show that» —
требование к записи.

**Ноябрь 2021 Q11(c)** — предел записан как (x²eˣ − x²)/x³, в бумаге
(x²eˣ − x²)³/x⁹, та же дробь в кубе. Ответ 1 в обоих случаях, поэтому
ошибка и уцелела. Блок принадлежит E1.

**Ноябрь 2023 TZ1 Q11(a)** — область −π/4 ≤ x ≤ π вместо −π/4 ≤ x ≤ 5π/4.
На пункт (d), который принадлежит E2, это не влияет — ряд строится около
нуля, — но третья стационарная точка вопроса лежит как раз за π.

**Ноябрь 2023, дубль.** Вопрос про e^(cos 2x) поставлен в TZ1 и TZ2 слово
в слово. Бумаги разные (8823-7106 и 8823-7111), совпадение в них самих;
ровно та же пара бумаг уже дала такой дубль в E1 — там это был sin²(kx)/x².
Отсюда 26 блоков и 99 баллов вместо 27 и 105.

Общее у первых четырёх одно: **ошибка не меняет ответ**. Предел в кубе даёт
ту же единицу, форма записи не меняет числа, деление на x меняет интеграл —
но только в тех пунктах, где ответ и так дан в условии. Разметку читали,
сверяя ответы, и всё, что сходилось по ответу, проходило.

### check_skills научился ловить тихий случай двоеточия

Карточка E2 не разобралась с первого раза — ровно на том, о чём
PRACTICUM.md предупреждает с A4: двоеточие внутри фразы списка. Это громкий
случай, `yaml.safe_load` падает, и `check_skills.py` его называет.

Но у той же ошибки есть тихая форма. Пункт вида

```yaml
      - Проверить свободный член: он равен произведению свободных членов
```

не роняет разбор — он молча превращает строку в отображение
`{'Проверить свободный член': 'он равен произведению свободных членов'}`.
Карточка «разбирается», приём выглядит целым, а в тренажёре на карте
приёмов пункт хода печатается как словарь.

`check_skills.py` теперь проверяет и это: каждый пункт `chain` и `traps`
обязан быть строкой. Проверка нашла **двенадцать таких пунктов в четырёх
уже готовых карточках** — calculus-limits, functions-curve-sketching,
functions-equations, geometry-trigonometry. Все взяты в кавычки.

### Лестница идёт по тому, откуда берётся ряд

| ступень | приём | что её вызывает |
|---|---|---|
| 1 | по определению | известный ряд не подходит, зато дан f⁽ⁿ⁾ или соотношение |
| 2 | подстановка в известный ряд | sin(x²), cos 2x, e^(−3x) |
| 3 | биномиальный ряд | скобка в дробной или отрицательной степени |
| 4 | произведение рядов | произведение двух знакомых функций |
| 5 | ряд внутри ряда | e^(cos 2x), sec x |
| 6 | почленно | «use integration to show», геометрическая прогрессия |
| 7 | из уравнения | y задан уравнением и y(0) |
| 8 | ряд вместо функции | «hence», интеграл без первообразной |
| 9 | насколько верно | «error», 0.0001, 1×10⁻⁶ |

Ступени 1–5 строят ряд и стоят 45 баллов; ступени 6–9 с ним работают
и стоят 54. **Разложение — та часть, которую все отрабатывают и которая
стоит меньше.** Арифметика после него — интеграл, значение π, оценка
ошибки — стоит больше и отрабатывается реже.

Приёмы 6 и 8 стоят рядом не случайно: и там и там ряд превращают в другой
ряд. Разница в том, зачем. В приёме 6 из ряда получают ряд, которого
не было; в приёме 8 ряд подставляют вместо функции и дальше он расходуется.

## E3, техника дифференцирования

[`practicum/calculus/practicum-e3-differentiation.ipynb`](practicum/calculus/practicum-e3-differentiation.ipynb)
— 41 ячейка, 188 баллов архива, [карточки приёмов](practicum/skills/calculus-differentiation.yaml).
Седьмой практикум серии и первый, где тему пришлось резать на три части.

### Тема на 350 баллов, разрезанная по вопросу, а не по функции

`calculus.differentiation` — 98 блоков и 350 баллов, вдвое больше верхней
границы. Резать её по типу функции бесполезно: цепное правило встречается
везде. Разрез сделан по тому, **что спрашивают**:

| куда | блоков | баллов | вопрос звучит так |
|---|---|---|---|
| E3 | 55 | 188 | найдите f′(x), покажите, что f″ = … |
| E4 | 33 | 131 | найдите касательную, под каким углом, кривая задана уравнением |
| E9 | 10 | 31 | найдите ускорение, скорость роста |

Доли в карте — 0.537 / 0.374 / 0.089 — это те же 188, 131 и 31 из 350,
а не подобранные числа. Списки блоков лежат поимённо в `blocks` карточек,
как это сделано у A5 и A6.

Разрез проходит внутри вопросов. У майской 2022 Paper 3 пункт (d)(i) просит
показать вид `g′(x)` и стоит в E3, а (d)(ii) строит по ней касательную и
стоит в E4. Это не неудобство разметки, а то, как устроен экзамен: одна
производная, два разных умения.

### Параметрического дифференцирования в архиве нет вовсе

Практикум E4 в карте назывался «Неявное и параметрическое дифференцирование».
При разборе выяснилось, что второй половины не существует: ни в одной из
семнадцати сессий не встречается кривая, заданная парой x(t), y(t), и
производная dy/dx = (dy/dt)/(dx/dt) не спрашивается ни разу. Название
исправлено на «Неявное дифференцирование: касательная и нормаль», и рядом
стоит `revisit` — пересмотреть, если следующие сессии её добавят.

Стоит помнить, откуда взялось прежнее название: из программы, а не из
архива. Карта строилась по темам курса, и в этом месте курс обещал больше,
чем спрашивают.

### Двенадцатое понятие равенства ответов: производная вычисляется

Понятие само по себе простейшее в серии. Производная у функции одна,
и `verify_derivative` получает её из условия — эталона не хранится, любая
эквивалентная запись проходит:

```python
verify_derivative('4', q4, x**n*(a - x)**n, params={n: (2, 3, 5)})
```

Новое здесь не это, а **разбор неверного ответа**. Промахи в этой теме
именные, и «не сходится» о них не говорит ничего:

| промах | что проверка отвечает |
|---|---|
| `cos(2x)` вместо `2cos(2x)` | потерян множитель — производная внутренней функции |
| `2e^(2x)·3` вместо `e^(2x)(6x−5)` | произведение продифференцировано как u′v′ |
| `(uv′ − u′v)/v²` | в частном перепутан знак числителя |
| `4x⁴` вместо `4x³` | показатель не понижен на единицу |
| сама функция | функция не продифференцирована |
| производная порядка n−1 | это производная порядка 1, а не 2 |

Каждый из этих промахов проверка **строит из самой f** — по тому же правилу,
применённому не так, как оно устроено. Списка неверных ответов не существует
так же, как не существует списка верных: и те, и другие выводятся из условия.
Функция `_slips(f, var, order)` разбирает запись `f` (`Mul` из двух
движущихся множителей, дробь с `x` в знаменателе, вложенная функция) и
собирает промахи, отвечающие её форме. Если ответ не совпал ни с одним,
печатается общее «не совпадает с производной функции из условия».

Одна тонкость, которая вылезла сразу. Проверка сравнивает функции численно,
и у √(1+x) при x < −1 обе записи ответа комплексны, но ветвь корня у них
разная — `-1/(4*sqrt((1+x)**3))` и `-1/(4*(1+x)**Rational(3,2))` расходятся
за пределами области. Точки, где значение не действительно, теперь
пропускаются: вопрос ставится на области, и проверять надо там же.

### Ещё две проверки

`verify_stationary(label, got, f, domain, order=1)` — для приёма 9. Точка
нулевого наклона это **пара** чисел, и половина потерянных баллов темы там,
где найдена только первая: «coordinates» значит две координаты, и
подстановка в саму f — отдельный балл. Проверка требует обе, а полноту
набора берёт независимым сканированием производной по отрезку из условия.
`order=2` переключает её на точки перегиба.

Сканирование пришлось чинить в ту же сессию. `_scan_roots` ищет смены знака
и концов отрезка не видит, а у cos²x − 3sin²x на [0, π] **две из трёх**
точек нулевого наклона стоят ровно на концах. Ответ из двух точек вместо
трёх проходил незамеченным ровно там, где вопрос ставится закрытым отрезком.
Поймал это не глаз, а `verify_e3.py`: типовая ошибка `[(0, 1), (pi, 1)]`
не была отвергнута.

`verify_constants(label, got, unknowns, conditions)` — для вопросов, где
буквы стоят внутри функции. Подставлять ответ некуда: `y = (x−4)/(ax²+bx+c)`
не проверишь, пока не знаешь a, b, c. Поэтому подставляется он в **условия
самого вопроса**, и каждое из них — одно предложение условия:

```python
verify_constants('12', q12, [a_, b_, c_], [
    ('the denominator is zero at the asymptote x = 1',
     (a_*x**2 + b_*x + c_).subs(x, 1)),
    ('the curve passes through (2, 1)', Eq(Y.subs(x, 2), 1)),
    ('and the gradient there is zero, because it is a minimum',
     diff(Y, x).subs(x, 2)),
])
```

Проверка называет первое нарушенное условие. Это ровно то, что делает
схема оценивания: три уравнения, и за каждое отдельный балл.

### Тест берёт производную конечной разностью, а не sympy

Здесь у `verify_e3.py` появилась причина, которой не было у предыдущих
практикумов. `verify_derivative` вызывает `sp.diff`; если тот же `sp.diff`
подтвердит его ответы, подтверждено будет только то, что sympy согласна
сама с собой.

Поэтому производная в тесте берётся **численно, прямо из определения** —
пятиточечной конечной разностью с погрешностью порядка h⁴:

```python
def slope(expr, var, point, step=1e-3):
    f = _fn(expr, var, subs)
    return (f(point - 2*step) - 8*f(point - step)
            + 8*f(point + step) - f(point + 2*step)) / (12 * step)
```

Символьного дифференцирования в тесте нет ни разу. Совпадение написанной
формулы с наклоном настоящей секущей и есть проверка. Та же машинка стоит
и в `verify_drill.py`, где ею пересчитаны все девять генераторов.

У неё нашлась своя ловушка, и тоже через провал теста. У дроби
(5x + 1)/(3x² − 1) полюс стоит в 1/√3 ≈ 0.5774, а пробная точка — в 0.57.
Производная там порядка 2·10⁴, следующие порядка 10⁹, и ошибка усечения
перестаёт быть малой: разность даёт −20736.858 против верных −20768.173,
расхождение в третьем знаке. Признак этого виден без всякой теории — **два
разных шага дают разные ответы**. Теперь наклон считается дважды, шагом h
и h/4, и точка, где они не сошлись, выбывает, а не отвергает эталон.

### Провал теста поймал неверное утверждение в разборе

Второй раз за серию (первый был в E2 с √3 ≈ 500/279). Я написал в решении
задания 8, что минимальная длина шеста ≈11.19 м «чуть меньше» шеста
в 11.25 м. Проверка `abs(L(arctan 2) − 11.19) < 0.01` провалилась.

Правда: L(arctan 2) = 3√5/4 + 3√5 = **15√5/4 ≈ 8.39 м**. Шест в 11.25 м
не проходит за угол не «чуть-чуть», а с большим запасом. И логику стоило
назвать вслух: минимум L — это длина самого длинного шеста, который
проходит, поэтому сравнивать надо именно с минимумом, а не с чем попало.
Разбор переписан.

### Иллюзия калькулятора, шестой раз и самый широкий

73% баллов формально с калькулятором — больше, чем у любого практикума
серии, кроме E7. Настоящей работы для GDC здесь на **15 баллов из 188**:

* два корня неравенства 4 + 2x − 3eˣ < 0 (май 2025 TZ2, 3 балла);
* два значения a из f′(1) = tan 70° (май 2025 TZ3, 4 балла);
* система трёх линейных уравнений (май 2024 TZ2, 8 баллов).

Всё остальное — Paper 3 с точным ответом: 45√5/4, −r²/y³, 2√3/9, 2|a|.
Тридцать четыре блока из пятидесяти пяти приходят из длинных исследований,
и там калькулятор лежит на столе включённым просто потому, что бумага
такая.

### Семь замечаний к разметке

Пять из них — расхождения с бумагой, и два из пяти делают вопрос неверным.

**`2021-MAY-TZ1-P2-Q11-B`.** Функция записана как (2x + 3)/(4x² − 1),
в бумаге (3x + 2)/(4x² − 1). Ошибка прошла и в `method_path`: числитель
производной там −8x² − 12x − 3, что соответствует записи корпуса.
У функции из бумаги производная (−12x² − 16x − 3)/(4x² − 1)².

**`2025-MAY-TZ2-P3-Q01-C`.** Потерян множитель x впереди: корпус пишет
(x² − A)/(x² + A), бумага — x(x² − A)/(x² + A). Разница не косметическая:
у записи корпуса f′(√A) = 1/√A, то есть **от A зависит**, и вопрос
«prove that f′(√A) is independent of A» становится неверным. У функции
из бумаги f′(√A) = 1.

**`2025-NOV-TZ1-P3-Q02-B-III`.** В `task_summary` потерян минус:
f′(x) = 1/(x ln(200/x)) вместо напечатанного −1/(x ln(200/x)). В
`method_path` минус на месте, так что ошибка только в пересказе — но
именно пересказ читает тот, кто собирает задание.

**`2021-MAY-TZ1-P1-Q04-C`.** k = (2e + e²)/4 ≈ 3.21 вместо напечатанного
k = e + e²/4 ≈ 4.57. Блок принадлежит E4, найден при сборке E3.

**`2023-NOV-TZ1-P1-Q11-A`.** Область −π/4 ≤ x ≤ π вместо −π/4 ≤ x ≤ 5π/4.
То же замечание уже стояло в карточке E2 — там оно касалось пункта (d).
На ответ не влияет: точки нулевого наклона лежат в 0, π/2 и π, следующая
была бы в 3π/2 и за границу не попадает. Поэтому и уцелело.

**Страницы смещены на единицу у семи блоков.** У майской 2024 Paper 3
указано «4-5», а вопрос 2 начинается на странице 5 PDF; у ноябрьской 2022
Paper 1 указано 11, вопрос 10 на странице 12. Смещение не общее — у
большинства бумаг страницы верны, — но при машинной подаче страниц в модель
(`practicum/drill/archive.py`) это даёт **чужой вопрос без единого признака
ошибки**. Стоит пересчитать `source_pages` сплошным проходом.

**Параметрического дифференцирования нет.** Разобрано выше.

### Лестница идёт по тому, что спрашивают

| # | приём | баллов |
|---|---|---|
| 1 | Степень и почленно | 7 |
| 2 | Таблица производных | 10 |
| 3 | Цепное правило | 18 |
| 4 | Произведение | 14 |
| 5 | Частное | 22 |
| 6 | До напечатанного вида | 10 |
| 7 | Вторая и высшие | 29 |
| 8 | Буква в функции | 27 |
| 9 | Прочесть производную | 51 |

Приёмы 1–5 выбирают правило, и выбор делается **чтением формы записи** до
всякого счёта. Они стоят 71 балл. Приёмы 6–9 стоят 117.

**Выбор правила — это четверть темы.** Дифференцирование — та часть,
которую все отрабатывают, и она стоит меньше всего; алгебра после него
и чтение результата стоят больше и отрабатываются реже. Та же форма была
у E2: построить ряд стоило меньше, чем им воспользоваться. Два практикума
подряд — уже не совпадение, а свойство секции.

**Приём 9 — самый крупный и не требует новой техники.** Пятьдесят один
балл, и вся работа после того, как производная уже есть: решить f′ = 0,
подставить корни обратно в f, посмотреть на знаки. Потери там не
математические — координата, названная одним числом; отрезок, прочитанный
короче; «decreasing», решённое как уравнение.

## D2, вероятность

[`practicum/statistics/practicum-d2-probability.ipynb`](practicum/statistics/practicum-d2-probability.ipynb)
— 35 ячеек, 156 баллов архива, [карточки приёмов](practicum/skills/statistics-probability.yaml).
Восьмой практикум серии, первый в секции D и первый по статистике вообще.

### Тринадцатое понятие равенства ответов: пространство определяет ответ

Здесь впервые ответ — просто число от нуля до единицы. Сверять такое
с записанным эталоном значило бы делать ровно то, чего практикумы избегают
с самого начала: у 0,543 нет ни формы записи, которую надо принять,
ни эквивалентных вариантов, ни структуры, в которую можно подставить.

Проверять есть что другое. **Вопрос о вероятности не даёт число — он даёт
пространство**, а пространство определяет ответ однозначно. Условия билета
это уравнения на веса исходов, и решить их — значит восстановить вопрос,
а не подсмотреть ответ:

```python
A, B = events('A B')
verify_event('2.2', q2_2,
             [(P(A), Rational(1, 2)), (P(B), Rational(1, 3)),
              (P(A, given=B), Rational(1, 4))],
             P(A, given=~B))
```

Два события режут пространство на четыре клетки диаграммы Венна, три —
на восемь. Любое событие, какое можно записать через A и B, есть
объединение клеток, поэтому пересечение, объединение и дополнение — это
операции над множествами индексов, и алгебру событий писать не пришлось
вовсе. `verify_event` собирает уравнения, отдаёт их sympy, отбрасывает
решения, где какой-нибудь вес вышел за [0, 1], и вычисляет то,
о чём спрашивают.

Запись выбрана так, чтобы ячейку можно было прочесть вслух как билет:
`A & B` — пересечение, `A | B` — объединение, `~A` — дополнение,
`P(A, given=B)` — то, что экзамен пишет как P(A|B). Вертикальную черту
под условную вероятность занять было нельзя: в Python она значит
объединение, и P(A | B) читалось бы двумя разными способами на одной
странице.

### Разбор неверного ответа: промахи строятся из того же пространства

То же, что в E3, и по той же причине: «не сходится» об этой теме
не говорит ничего.

| промах | что проверка отвечает |
|---|---|
| P(B\|A) вместо P(A\|B) | условная вероятность взята в обратную сторону |
| P(A∩B) вместо P(A\|B) | делить на вероятность условия ещё не стали |
| P(A) + P(B) вместо P(A∪B) | пересечение посчитано дважды |
| P(A)·P(B) вместо P(A∩B) | независимость в условии не обещана |
| 1 − p вместо p | это вероятность противоположного события |
| что угодно больше единицы | вероятность не бывает больше единицы |

Ни один из них не записан. `_prob_slips` смотрит, **о чём именно
спросили** — `P(A, given=B)` помнит, что это условная вероятность и какие
у неё аргументы, а `A & B` помнит, что это пересечение и чего с чем, —
и строит промах тем же определением, применённым не так, как оно устроено.

### Ещё две проверки

`verify_probability(label, got, space, find, given)` — там, где
пространство проще перечислить, чем вывести: дерево, повторные испытания,
вынимание без возвращения, 216 равновозможных троек. `space` это словарь
«исход → вес», `find` и `given` — предикат или набор исходов.

Первым делом она проверяет, что все веса вместе дают единицу. Это не
формальность: **неверно переписанное дерево ловится именно здесь**, до
всякого счёта, и сообщение говорит, чему сумма равна на самом деле.

`verify_independence(label, got, given, a, b)` — для вопросов «независимы
ли». Вердикт сам по себе монета: угадать его можно и не считая, а проверка,
принимающая монету, ничего не проверяет. Схема оценивания и не даёт за
вердикт отдельного балла — R-балл зависит от предыдущего, а тот стоит
на двух числах. Их и просят: `[P(A)·P(B), P(A∩B)]`, в этом порядке.
Вывод из них следует сам, и проверка его называет.

### Проверка узнала, что буква обязана быть вероятностью

Найдено провалом теста. У майской 2024 TZ2 вопрос заканчивается так:
оба корня, k = 1/3 и k = 8/3, удовлетворяют уравнению 9k² − 27k + 8 = 0,
и надо сказать, почему верен первый. Типовая ошибка `[Rational(8, 3)]`
не была отвергнута — и правильно: `verify_constants` подставляет число
в условия вопроса, а условию (1−k)(1−k/2) = 5/9 восьмёрка третьих
удовлетворяет точно.

У `verify_constants` появился аргумент `domain`. Обычно он пустует: буквы
внутри кривой ищут точными и лежать им негде. Но когда буква стоит внутри
вероятности, отбраковка корня — это отдельный балл схемы оценивания
(«any valid reasoning indicating that any probability cannot be greater
than 1»), и проверка, которая её не знает, засчитала бы неверный ответ.

### Тест берёт вероятность частотой, а не алгеброй

Причина та же, что у конечных разностей в E3. `verify_event` восстанавливает
пространство, решая систему; если ту же систему решит и `verify_d2.py`,
подтверждено будет только то, что sympy согласна сама с собой.

Поэтому вероятности в тесте берутся **долей в длинной серии**. Пишется
генератор, повторяющий историю вопроса — выбрать коробку и вынуть шар,
катить кость, пока не выпадет успех, вынуть три шара и не класть обратно,
бросить три кости, — и событие считается по 400 000 повторений:

```python
def draw10(rng):
    name = rng.choice(['A', 'B'])
    q = 0.1 if name == 'A' else 0.05
    return name, sum(1 for _ in range(10) if rng.random() < q)

share, base = frequency(draw10, lambda o: o[0] == 'A',
                        given=lambda o: o[1] == 2)
```

Совпадение вычисленной вероятности с долей и есть проверка: это само
определение вероятности, а не другой способ её посчитать. Допуск — пять
стандартных ошибок от биномиального разброса, то есть около 0,004 при
400 000 испытаниях, и все типовые промахи отстоят от верного ответа
на порядок больше.

Задачи 1–5 и таймер разыграть нельзя: они дают готовые P(A), P(B), P(A∩B)
и никакой истории. Там арифметика выписана в тесте руками, числом за числом,
с указанием, откуда каждое взялось. Это тоже два разных пути: ноутбук
решает систему, тест складывает клетки.

Та же машинка стоит и в `verify_drill.py`, где ею пересчитаны все десять
генераторов: пространство задания разыгрывается по весам, и ответ
сверяется с частотой.

### Провал теста поймал арифметическую ошибку в архиве

Третий раз за серию. В разделе 7.5 архива я выписал доли нормального
распределения как 0,4258638 / 0,4550135 / 0,1191227 и получил ответ 0,792.
`check_archive_d2.py` сказал «не совпадает с тем, что даёт пространство»,
и был прав дважды: сумма долей единицы не давала, а верные значения —
0,4258633 / 0,4619448 / 0,1121919, и ответ 0,805. Схема оценивания
ноябрьской 2022 печатает 0,804590.

Поймала это именно проверка суммы весов, поставленная в `verify_probability`
первой строкой. Она задумывалась против неверно переписанного дерева
и сработала против неверно посчитанного интеграла.

### Условий бывает меньше, чем клеток, и это не ошибка

Тоже провал теста, и тоже в первом же прогоне. У майской 2025 TZ2 даны
P(A∪B) = 5/8 и P(A∩B′) = 7/24 — два условия на четыре веса. Пространство
действительно не определено: P(A∩B) может быть каким угодно. А P(B),
о котором спрашивают, определено, потому что от неверных весов не зависит:
A∪B распадается на A∩B′ и B, и они не пересекаются.

`_solve_space` сначала отбрасывала такие решения целиком. Теперь она
возвращает их как есть, а определённость проверяется **у самого ответа**:
если после подстановки в нём не осталось ни одного веса-буквы — вопрос
поставлен корректно, и это надо было различать с самого начала. Сообщение
«условий не хватает: они допускают разные ответы» осталось для случая,
когда ответ и правда плавает.

### Крайнее значение ищется по вершинам многогранника

Ноябрьская 2022 Paper 1 Q6 даёт только P(A) = 0,3 и P(B) = 0,8 и спрашивает
наименьшее и наибольшее возможное значение P(A∩B). Пространство здесь
не определено намеренно, и вопрос именно об этом.

Множество допустимых весов — выпуклый многогранник: сумма единица,
равенства вопроса, каждый вес неотрицателен. Крайние значения линейной
величины на многограннике достигаются в вершинах, и вершины перебираются
честно: занулить подходящее число весов и решить оставшуюся линейную
систему. Флаг `extreme='min'` или `'max'` включает этот путь.

Ответы — 0,1 и 0,3 — выходят точными, и хранить их не пришлось.

### 62% с калькулятором, и это самая честная доля за десять практикумов

Впервые за серию иллюзии почти нет. Настоящей работы для GDC здесь около
30 баллов из 156: нормальные вероятности на ветвях у майской 2022 TZ1
и ноябрьской 2022, биномиальные — у майской 2025 TZ3 и ноябрьской 2025 TZ1.
На бумаге они не берутся, и это не поза.

Остальные 126 баллов — арифметика с двумя знаками после запятой и
перебор по случаям. Двадцать один блок из пятидесяти вообще из Paper 1.

### Восемь замечаний к разметке

Четыре из них одного вида и одного рода вреда.

**Смещение `source_pages` на единицу, четыре раза.** У 2021-MAY-TZ2-P2-Q03
указана страница 4, а вопрос на пятой (на четвёртой Q2 про арифметическую
прогрессию). То же у 2022-NOV-COMMON-P1-Q06 (7 вместо 8),
2022-MAY-TZ2-P2-Q03 (5 вместо 6) и 2024-MAY-TZ1-P2-Q06 (8 вместо 9).
Схема оценивания при этом указана верно во всех четырёх.

Это единственная из находок, которая ломает не текст, а работающий код:
`practicum/drill/archive.py` отдаёт модели картинку страницы по этому
номеру, и модель получает **чужой вопрос без единого признака ошибки**.
Четыре случая в одной теме — повод проверить весь корпус разом, а не
править по одному.

**Потерянное условие.** У 2025-MAY-TZ1-P1-Q04-A `task_summary` говорит
«given P(A') = 3/4 and P(B|A) = 2/3», а в билете дано
P(A′) = P(A∪B) = 3/4. Без P(A∪B) пункт (b) не решается вовсе.

**Потерянное слово «given».** У 2024-MAY-TZ2-P1-Q06-B пересказ звучит как
полная вероятность, а вопрос условный: это Байес.

**Вопрос не в своей теме.** 2023-MAY-TZ2-P2-Q11-C («find two pairs of
positive integer values for r and y») числится за
`number_algebra.polynomials`. Целочисленное решение там есть, но вопрос
про вынимание без возвращения, и метод у него этой темы.

**Ноябрьский дубль 2023 года**, в третий раз за серию: Q2 посчитан дважды
двумя зональными копиями одной бумаги. Четыре балла.

### Описательной статистики нет в карте вовсе

Ошибка того же рода, что параметрическое дифференцирование в E4, но
обратная. Там карта обещала то, чего в архиве нет; здесь **архив
спрашивает то, чего в карте нет**.

Двенадцать баллов темы — ящик с усами и межквартильный размах (май 2021
TZ1 Q3), среднее, медиана и размах (ноябрь 2025 TZ3 Q2), способ отбора
(май 2025 TZ3 Q3) — не принадлежат ни одному из семи практикумов секции D.
Ни комбинаторике, ни вероятности, ни распределениям, ни регрессии.

На отдельный практикум двенадцати баллов не хватает: нижняя граница 60.
Вероятнее всего их следует приписать к D7, где сейчас только регрессия
и корреляция (69 баллов), и переименовать его во что-то вроде
«Описательная статистика, регрессия и корреляция». Записано в `revisit`
карточки; решать при сборке D7.

### Доля темы, посчитанная заново

`statistics.probability` — 77 блоков и 237 баллов. D2 берёт 50 блоков
и 156 баллов, доля 0.658 вместо прежних 0.55 из карты. Оставшийся 81 балл
достался D3, и его доля исправлена с 0.45 на 0.342 — но с честной
припиской в `revisit`: собственно биномиальных среди этих 82 баллов
только 36, а остальные распадаются по D4, D5, D6, дублю и описательной
статистике. Тридцати шести баллов на практикум не хватает, и D3 придётся
добирать из `statistics.discrete_random_variables` при сборке.

### Лестница идёт по тому, что приходится делать с пространством

| # | приём | что делают с пространством |
|---|---|---|
| 1 | четыре клетки | читают готовое |
| 2 | условная вероятность | отрезают кусок и читают в нём |
| 3 | независимость | получают из него уравнение |
| 4 | дерево | строят по этапам |
| 5 | первый успех | строят повторением |
| 6 | полная вероятность | проходят вниз |
| 7 | Байес | проходят вверх |
| 8 | без возвращения | строят с убывающим знаменателем |
| 9 | перебор исходов | строят счётом |
| 10 | буква внутри | ищут в нём неизвестное |

Приёмы 1–3 живут на одной картинке и по сути одно умение, увиденное
трижды. Приёмы 6 и 7 — одна машинка в двух направлениях. Отдельны
по-настоящему 4, 5, 8 и 9 — четыре способа построить пространство —
и 10, единственное место темы, где ответ отбраковывают не за то,
что он не решает уравнение, а за то, что он не вероятность.

## Архивный ноутбук: весь корпус темы подряд

Формат, собранный поверх B4 и повторённый на C3, B5, E1, E2, E3 и D2. Практикум учит: лестница из
приёмов, теория перед каждым, три уровня сложности, тренажёр распознавания, задание
на время. Он берёт из корпуса не всё, а то, на чём приём виден лучше всего, и
часто сводит три архивных вопроса в одно задание.

Архивный ноутбук не учит. Он даёт набивать руку: **вся тема подряд, по тем же
приёмам, без единой строчки теории.** Открыл, решил пятьдесят вопросов,
закрыл. Таких ноутбуков семь: B4, C3, B5, E1, E2, E3 и D2.

```
practicum/generators/build_archive_b4.py   → practicum/functions/archive-b4-curve-sketching.ipynb
practicum/generators/build_archive_c3.py   → practicum/geometry/archive-c3-trigonometric-equations.ipynb
practicum/generators/build_archive_b5.py   → practicum/functions/archive-b5-exponential-log-models.ipynb
practicum/tests/check_archive_b4.py        прогоняет свой ноутбук дважды
practicum/tests/check_archive_c3.py        то же самое
practicum/tests/check_archive_b5.py        то же самое
```

Ниже сначала B4 — на нём формат придуман, — потом то, что изменилось на C3 и B5.

### Первый ноутбук: B4

**54 вопроса, 127 баллов.** В корпусе у темы 62 блока и 147 баллов; разница —
ноябрьская сессия 2023 года, которая лежит двумя зональными копиями одной
бумаги. Дубли сведены, и остаётся ровно то, что на самом деле спрашивали. То
самое «около 127 баллов» из карточки перестало быть оценкой и стало
содержанием файла.

Раскладка по приёмам — карточка `functions-curve-sketching.yaml`, поле `blocks`
у каждого приёма. Никакой отдельной разметки формат не заводит: он читает ту,
что уже есть.

| § | приём | вопросов | баллов |
|---|---|---|---|
| 1 | асимптоты, читаемые с дроби | 8 | 12 |
| 2 | асимптота через предел | 2 | 3 |
| 3 | наклонная асимптота | 4 | 13 |
| 4 | эскиз дроби | 10 | 26 |
| 5 | множество значений | 4 | 13 |
| 6 | эскиз с подписями | 11 | 33 |
| 7 | эскиз по второй производной | 1 | 3 |
| 8 | кривая, которая не функция | 5 | 10 |
| 9 | счёт корней у семейства | 9 | 14 |

### Что оказалось важным в формате

**Вопрос — из бумаги, не из корпуса.** Все 54 условия набраны с оригинальных
страниц: страницы рендерятся из PDF картинками (`practicum/drill/archive.py`) и
читаются глазами. Пересказ из корпуса для этого не годится — он получен
извлечением, и в B4 доказано шесть расхождений подряд.

**Одна строка на ответ.** В практикуме эскиз вводят словарём на пять строк с
именами ключей; здесь это одна строка `q4_1 = {...}` и комментарий, который
перечисляет нужные ключи. Разница не косметическая: при пятидесяти четырёх
заданиях подряд прокрутка становится частью упражнения, и её надо убрать.
Заодно однострочный placeholder делает возможным тест ниже.

**Проверки те же, и эталона по-прежнему нет.** `verify_asymptotes` считает
предел, `verify_sketch` пересчитывает особенности, `verify_range` спрашивает
достижимость, `verify_param_set` опрашивает семейство. Хеш понадобился шесть
раз из пятидесяти семи — там, где ответ это число из markscheme, которое
проверке неоткуда вывести: вторая производная `e^(cos 2x)` в приёме 7 и наклон
ветви `y² = x³` в приёме 8.

**Четыре вопроса проверить нельзя, и они оставлены как есть.** Две «назовите
два ключевых признака», одно «выше или ниже оси» и доказательство на пять
баллов. Формат не притворяется, что умеет их проверить: у них нет ячейки, а в
разборе выписан весь список, который принимает markscheme, — вместе с тем,
что markscheme отказывается принимать («не засчитывать „они все симметричны“
без указания оси симметрии»).

**Один вопрос удалось перевести в проверяемый.** Приём 7 целиком про форму
кривой между особенностями — то единственное, чего `verify_sketch` не видит.
Вместо картинки спрашиваются три числа: `f''(0) = −4e`, `f''(π/2) = 4/e` и их
отношение `e²`. «Во сколько раз максимум острее минимума» — это и есть
`relative values` из условия, и это проверяемо.

### Тест: ноутбук прогоняется дважды

У архивного ноутбука нет ни одного разбора, который проверялся бы отдельно:
вся его правильность в том, что ответ из раздела Solutions проходит проверку в
ячейке. Отсюда и устройство `check_archive_b4.py`.

Генератор держит словарь `ANSWERS` — эталонный ответ для каждого placeholder.
В ноутбук он не попадает. Тест берёт сам собранный `.ipynb`, подставляет
эталоны построчно и исполняет все ячейки подряд в одном пространстве имён.

Первый прогон — пустой: должен пройти сверху вниз без единого исключения и
напечатать 57 знаков `⬜`. Это не формальность, а условие публикации: Kaggle
исполняет ноутбук при заливке, и падение на незаполненном `...` даёт статус
`ERROR`.

Второй прогон — с эталонами: 57 проверок, все `✅`, ни одного `❌`. Поскольку
эталоны взяты из markschemes, а проверки эталонов не хранят, совпадение здесь
означает согласие markscheme с самой функцией — то же независимое
подтверждение, что дают `verify_*.py` у практикумов, только полученное разом.

### Второй ноутбук: C3

**35 вопросов, 129 баллов.** Корпус числит за темой 45 блоков и 176 баллов,
и разница здесь больше, чем в B4, потому что вычитается не только дубль:

| откуда | блоков | баллов |
|---|---|---|
| не эта тема: комплексные числа и вопросы без тригонометрии | 7 | 28 |
| ноябрьский дубль 2023 года (Q12(c) посчитан дважды) | 1 | 7 |
| тождества по Муавру, живущие в A6 | 3 | 14 |
| добавлено обратно: sin 75° (в корпусе тема блока другая) | +1 | +2 |

| § | приём | вопросов | баллов |
|---|---|---|---|
| 1 | опорный угол и симметрия окружности | 2 | 5 |
| 2 | составной аргумент | 2 | 9 |
| 3 | формулы сложения углов | 5 | 15 |
| 4 | пифагорово тождество и квадратное уравнение | 5 | 18 |
| 5 | двойной угол и квадратное уравнение | 7 | 27 |
| 6 | разложение на множители вместо деления | 3 | 16 |
| 7 | сведение к тангенсу | 2 | 10 |
| 8 | отбор корней | 4 | 19 |
| 9 | численное решение | 5 | 10 |

Что изменилось по сравнению с B4.

**Секции бывают крошечными, и это содержание, а не дефект.** На первый приём
темы — опорный угол — в архиве приходится пять баллов из ста двадцати девяти.
Он почти никогда не бывает целым вопросом: он последние две строки почти
каждого другого. Формат это показывает прямо, вместо того чтобы дописывать
задания до ровного счёта.

**Хешей девять из тридцати семи против шести из пятидесяти семи в B4.** Восемь
из девяти — одна и та же причина: калькуляторный ответ округлён до трёх
значащих цифр, а такое число уравнению точно не удовлетворяет, и подставить
его некуда. Это свойство темы, а не проверок: у B4 ответом была прямая или
множество, у C3 — угол.

**Зато корни проверяются лучше всего в серии.** `verify_roots` подставляет
каждый корень в само уравнение и независимо сканирует отрезок, поэтому ловит
ровно ту ошибку, ради которой тема существует: корни верны, но найдены не все.
Из тридцати семи проверок пятнадцать такие.

**Два вопроса пришлось переписать под проверку, и оба честно.** «Найдите
наименьшее положительное x» превращено в «единственное решение на (0, 3π)»
(там оно и есть наименьшее), а «найдите A и C» — в «найдите все три точки
пересечения, включая данную вам B»: именно B теряется при делении на cos x,
и спросить про неё полезнее, чем обойти.

**Чтение бумаг снова поправило разметку.** Корпус записывает
2025-MAY-TZ2-P1-Q08-B как `arccos x + arccos(3x) = 3π/2`; в бумаге стоит
`arccos x + arccos(x√3)`. С искажённой записью задача неразрешима, и прежнее
замечание в `corpus_issues` карточки было написано против неё — заменено.

### Третий ноутбук: B5

**36 вопросов, 83 балла.** Корпус числит за темой 37 блоков и 85 баллов, и
разница ровно одна: `2025-NOV-TZ3-P1-Q08-A`, помеченный темой
`mathematical_models`, но без единой показательной или логарифмической функции
внутри. Больше ничего не вычтено и ничего не добавлено — впервые архивный
ноутбук совпадает с темой корпуса почти буква в букву.

| § | приём | вопросов | баллов |
|---|---|---|---|
| 1 | три закона логарифма | 3 | 7 |
| 2 | смена основания | 3 | 10 |
| 3 | уравнение с неизвестным под логарифмом | 1 | 4 |
| 4 | законы показателей и данное соотношение | 3 | 5 |
| 5 | логарифм обеих частей | 3 | 7 |
| 6 | чтение готовой модели и подгонка новой | 7 | 15 |
| 7 | проценты как степень | 5 | 13 |
| 8 | логистическая модель | 5 | 8 |
| 9 | логарифм как функция | 6 | 14 |

Что изменилось по сравнению с C3.

**Хешей четыре из тридцати семи проверок против девяти из тридцати семи.**
Ровно один случай на все четыре: ответ является округлением (2380 лет, 10 100
человек, 1041 динар, €5906.23), и округлённое число ничему не удовлетворяет
точно. Всё остальное проверяется по существу: `verify_model` подставляет данные
из условия, `verify_exact` требует точной формы и отвергает десятичную запись,
`verify_in_terms_of` требует, чтобы в ответе стояли данные буквы.

**Три вопроса не имеют ячейки вовсе, и это новое.** У C3 такой был один — «show
that» на один балл. Здесь их три: 5.1 просит показать `A₀ = 100` (после
подстановки `t = 0` проверять нечего), а 8.1 и 8.5 просят назвать допущение
и прокомментировать два предсказания — словами. Прятать ответ на них негде,
и вместо ячейки в вопросе стоит явная строка «this question has no cell»,
а сам ответ живёт в разборе. Раньше хотелось дописать туда какую-нибудь
проверку ради ровного счёта; правильнее оказалось сказать вслух, почему её нет.

**Секция может состоять из одного вопроса.** Приём «уравнение с неизвестным под
логарифмом» — сердце первой половины темы, и в архиве на него приходится один
вопрос на четыре балла. То же наблюдение, что дала C3 своим приёмом опорного
угла: приём живёт внутри других вопросов, а целым вопросом почти не бывает.

**Раздел 6 назван честно.** Шесть из семи его вопросов — подстановка чисел
в модель, которую построил кто-то другой, и только седьмой строит свою. Это
не разные приёмы, а начало и конец одного: подгонка начинается с того, что
модель умеют читать. Поэтому раздел называется «Reading a model, and fitting
one», а не «Fitting a model» с шестью разминочными заданиями впереди.

### Четвёртый ноутбук: E1

**24 вопроса, 73 балла.** Корпус числит 22 блока и 81 балл; разница — два
дубля, разобранных выше. Ноутбук впервые крупнее корпуса по числу вопросов
и меньше по числу блоков: несколько блоков разрезаны по пунктам (у одного
трёхбалльного блока пункты (f)(i) и (f)(ii) отвечают разными приёмами и
разъехались по секциям 1 и 2), а два блока склеены обратно в один вопрос.

| § | приём | вопросов | баллов |
|---|---|---|---|
| 1 | подстановка и сокращение | 1 | часть из 3 |
| 2 | старшая степень на бесконечности | 2 | часть из 6 |
| 3 | правило Лопиталя, один раз | 3 | 8 |
| 4 | правило Лопиталя, несколько раз | 3 | 18 |
| 5 | ряд Маклорена вместо правила | 3 | 9 |
| 6 | постоянная под конечный предел | 3 | 10 |
| 7 | предел по параметру | 3 | 9 |
| 8 | правило с буквой внутри выражения | 3 | 9 |
| 9 | что предел значит | 2 | часть из 5 |

Что изменилось по сравнению с B5.

**Хешей два из двадцати трёх против четырёх из тридцати семи.** Причина
в самой теме: предел проверяется приближением к нему, и эталона не нужно.
Осталось два случая, где ответ не предел, а постоянная, подобранная под него.

**Столбец «баллов» впервые говорит «часть из».** Разрезав трёхбалльный блок
по пунктам, честно указать долю нельзя: схема оценивания баллы по пунктам
не разносит. Писать выдуманное число хуже, чем сказать «часть из 3».

**Секция может состоять из одного вопроса** — то же наблюдение, что дали C3
и B5, третий раз подряд. Подстановка с сокращением — первая ступень темы
и её обязательное начало, а целым вопросом на неё в архиве приходится один
пункт.

**Один вопрос без ячейки.** 9.1 просит назвать словами, почему предел скорости
не имеет смысла для бегуньи на дистанции в 200 метров. Ответ — предложение,
и вместо ячейки в вопросе стоит явная строка «this question has no cell».

### Пятый ноутбук: E2

**27 вопросов, 99 баллов.** Корпус числит 27 блоков и 105 баллов; разница —
ноябрьский дубль, разобранный выше. Вопросов при этом столько же, сколько
блоков, но не тех же самых: ноябрьский вопрос про e^(cos 2x) разрезан по
пунктам — (d)(i) отвечает подстановкой и стоит в секции 2, (d)(ii) и (d)(iii)
подставляют ряд в ряд и стоят в секции 5.

| § | приём | вопросов | баллов |
|---|---|---|---|
| 1 | по определению | 4 | 13 |
| 2 | подстановка в известный ряд | 2 | 2 + часть из 6 |
| 3 | биномиальный ряд | 1 | 5 |
| 4 | произведение рядов | 5 | 21 |
| 5 | ряд внутри ряда | 2 | 4 + часть из 6 |
| 6 | почленно | 3 | 12 |
| 7 | из уравнения | 1 | 3 |
| 8 | ряд вместо функции | 6 | 20 |
| 9 | насколько верно | 3 | 13 |

Что изменилось по сравнению с E1.

**Четыре вопроса без ячейки против одного.** У всех четырёх ответ напечатан
в самом условии — «show that the Maclaurin series for sec x … is
1 + x²/2 + 5x⁴/24». Проверять нечего, и вопрос отправляют читать решение;
это тот самый открытый вопрос про «покажите, что», который стоит в конце
этого файла с самого начала серии.

**Хешей шесть из двадцати трёх против двух.** Причина разобрана выше:
вторая половина темы состоит из приближений, а приближение подставлять
некуда.

**Две секции держатся на одном вопросе Paper 3.** Секции 8 и 9 наполовину
и целиком собраны из ноябрьского исследования 2025 года про arctan и π —
шесть блоков, 21 балл, единственное место архива, где спрашивают об ошибке.
Такой зависимости от одного вопроса в серии ещё не было, и она честно видна
в ссылках: шесть подряд «November 2025 TZ3 Paper 3 Q2».

### Шестой ноутбук: E3

**55 вопросов, 188 баллов.** Дублей здесь нет вовсе — впервые за шесть
ноутбуков число вопросов равно числу блоков.

| § | приём | вопросов | баллов |
|---|---|---|---|
| 1 | степень и почленно | 5 | 7 |
| 2 | таблица производных | 3 | 10 |
| 3 | цепное правило | 7 | 18 |
| 4 | произведение | 4 | 14 |
| 5 | частное | 5 | 22 |
| 6 | до напечатанного вида | 4 | 10 |
| 7 | вторая и высшие | 6 | 29 |
| 8 | буква в функции | 9 | 27 |
| 9 | прочесть производную | 12 | 51 |

Что изменилось по сравнению с E2.

**Напечатанный ответ больше не означает «без ячейки».** В E2 таких вопросов
было четыре, и все четыре ячейки не получили: ряд там надо было угадать,
и напечатанный ответ отвечать было нечем. Здесь иначе. Половина вопросов
темы — «show that f′(x) = …», и написать производную и есть вся работа.
Ячейка не сторожит, а сообщает, сошлась ли алгебра, и потому стоит даже
там, где ответ виден в условии. Ноутбук говорит об этом прямым текстом,
включая то, что списать оттуда можно.

Без ячейки остались четыре вопроса, у которых **сдавать нечего**: два
«покажите, что сумма равна такой-то сумме» (ответ — сумма с многоточием),
одна перестановка тождества и одно неравенство «все множители
положительны».

**Записанный эталон — в трёх местах из пятидесяти пяти.** Хеш набора корней
{0, a/2, a} в 9.1 (подставить его некуда: уравнение записано через n),
2|a| в 8.3 и 1/r в 7.6. Остальные пятьдесят два вычисляются из условия при
каждом запуске ячейки — включая шесть, где `want` считается тут же:
`diff(G, x).subs(x, -1)`, `maximum(kv, x, Interval(0, oo))`,
`simplify(diff(L, alpha, 2).subs(alpha, arctan(2)))`.

**Две секции держатся на одном исследовании.** Восемь из девяти вопросов
секции 8 и два из шести секции 7 — майское исследование 2025 года про
кривизну, 29 баллов. Такое уже было в E2 (ноябрьский arctan и π), и здесь
это видно так же честно: девять подряд «May 2025 TZ1 Paper 3 Q2».

### Седьмой ноутбук: D2

**50 вопросов, 156 баллов.** Корпус числит за темой 77 блоков и 237 баллов,
и разница здесь больше, чем где бы то ни было, потому что вычитается
в основном **не эта тема**:

| откуда | блоков | баллов |
|---|---|---|
| биномиальное распределение, это D3 | 10 | 36 |
| производящая функция вероятностей, D4 | 3 | 11 |
| нормальное распределение, D5 | 3 | 7 |
| плотность непрерывной величины, D6 | 3 | 11 |
| ноябрьский дубль 2023 года | 2 | 4 |
| описательная статистика, которой в карте нет | 6 | 12 |

| § | приём | вопросов | баллов |
|---|---|---|---|
| 1 | алгебра событий | 8 | 17 |
| 2 | условная вероятность | 7 | 22 |
| 3 | независимость | 5 | 17 |
| 4 | деревья | 4 | 10 |
| 5 | повторные испытания | 7 | 17 |
| 6 | полная вероятность | 3 | 11 |
| 7 | Байес | 5 | 20 |
| 8 | без возвращения | 3 | 12 |
| 9 | перебор равновозможных | 5 | 18 |
| 10 | буква внутри вероятности | 3 | 12 |

Что изменилось по сравнению с E3.

**Ни одного записанного эталона на пятьдесят вопросов.** У B4 хешей было
шесть из пятидесяти семи, у E2 шесть из двадцати трёх, у E3 три из
пятидесяти пяти. Здесь — ноль. Причина не в аккуратности, а в теме:
вероятность **всегда** выводится из пространства, а пространство всегда
можно выписать. Даже «покажите, что вероятность равна 5/216» проверяется
пересчётом по 216 тройкам, и напечатанное в билете число входит в ячейку
как ответ студента, а не как эталон.

**Один вопрос без ячейки из пятидесяти.** «Explain why Y ≤ 5» — одна
фраза и один балл, и передавать нечего. У B4 таких было четыре из
пятидесяти четырёх, у E3 четыре из пятидесяти пяти.

**«Show that» здесь ячейку сохраняет всегда.** Правило, сформулированное
на E3, работает и тут: напечатанный ответ отменяет ячейку, только когда он
не выражение. Пять вопросов темы печатают свой ответ — 5/216, 0,288,
9k² − 27k + 8 = 0, 2y² − 2(r+1)y + r − r² = 0 — и все пять сохраняют
ячейку, потому что во всех пяти работа состоит ровно в том, чтобы
до напечатанного дойти, а ячейка говорит, дошли ли.

**Проверка суммы весов оказалась полезнее, чем задумывалась.** Она стояла
против неверно переписанного дерева; поймала неверно посчитанные доли
нормального распределения в 7.5 (см. главу про D2 выше).

### Сборка и публикация

`map.yaml` знает архивный ноутбук по двум полям рядом с практикумом:
`archive` и `kaggle_archive`. Тема одна, разъезжаться им незачем.

```bash
python practicum/generators/build_archive_d2.py
python practicum/tests/check_archive_d2.py
python practicum/make_kaggle.py D2:archive --push
python practicum/make_pdf.py D2:archive
```

Суффикс `:archive` понимают `make_kaggle.py` и `make_pdf.py`; `--all` по-прежнему
означает только практикумы.

## Чего в карте пока нет: системы линейных уравнений

Разбор, который стоит держать под рукой, потому что он про устройство карты,
а не только про одну тему.

Вопросы «решите систему трёх уравнений и скажите, когда решение единственно,
когда его нет и когда их бесконечно много» — это AHL 1.16, отдельный пункт
программы. В архиве их шесть, 18 баллов. Своего практикума из этого не собрать:
до нижней границы 60 далеко.

Хуже, что **по карте они не попадают никуда**. В `classification/taxonomy/topics.yaml`
нет подтемы под системы, классификатору некуда было их положить, и блоки
разъехались: три в `geometry.vectors`, один в `number_algebra.inequalities`,
один в `polynomials`. То есть вопрос из Paper 1 про то, при каких α и β система
имеет бесконечно много решений, уезжает в практикум про неравенства.

Разбор A8 это подтвердил с другой стороны: в теме неравенств нашёлся ещё один
такой блок, `2025-NOV-TZ3-P2-Q11-B-II` — решить два линейных уравнения на `a`
и `b`. На отдельный вопрос программы он не тянет (2 балла внутри задачи
про непрерывную случайную величину), но лежит там же и по той же причине.

Причина именно в таксономии, а не в отдельных ошибках разметки: пока подтемы
нет, новые сессии будут расползаться так же. Предложение завести
`number_algebra.systems_of_equations` и перечень блоков лежат в `corpus_issues`
карточки [`practicum/skills/geometry-vectors-3d.yaml`](practicum/skills/geometry-vectors-3d.yaml).

Пока тема не набрала на практикум, метод описан приёмом `linear_system` в C6.
Пара там точная: решить систему трёх уравнений и определить взаимное
расположение трёх плоскостей — одно действие на двух языках. «Нет единственного
решения» значит «общей точки нет», «бесконечно много» значит «пересекаются
по прямой».

**Отсюда общее наблюдение.** Карта построена по темам, но есть приёмы,
проходящие сквозь темы. Системы — самый чистый пример: 410 баллов архива
так или иначе содержат решение системы, но почти всегда это средний шаг
чужой задачи, а не вопрос. Такие вещи не становятся практикумом, они
становятся приёмом в том практикуме, где встречаются осмысленнее всего.
Признак, по которому это видно: тема не набирает 60 баллов, но её теги
встречаются в половине разделов.

**И что за этим смотреть.** Пять блоков из шести — из сессий 2025 года,
а в двенадцати полных сессиях 2021–2024 таких вопросов нет ни одного.
Если следующие сессии добавят ещё, тема наберёт на свой практикум,
и тогда её надо выносить в секцию A, а не держать приёмом в C6.
Пометка `revisit` у C6 в карте про это.

## Порядок работы над темой

1. Выбрать практикум из `map.yaml`, убедиться, что он в диапазоне.
2. Выгрузить его блоки из `classification/generated` по темам из карты.
3. Сгруппировать `method_tags` в 5–9 приёмов; на этом шаге сразу видны
   синонимы и провалы классификации — их стоит записать в `classification/reviews`.
4. Написать карточки в `practicum/skills/`, обязательно заполнив `trigger` и
   `traps`; `chain` собирается из `method_path`, `traps` — из markscheme.
5. Выстроить приёмы в лестницу. Если лестница не строится, разрез сделан не там —
   вернуться к шагу 1.
6. Подобрать по 1–2 вопроса на ступень, следя за балансом Paper 1 (без
   калькулятора) и Paper 2.
7. Собрать ноутбук и проверить три вещи: все эталонные ответы дают `✅`,
   типовые ошибки дают `❌`, и ноутбук проходится сверху вниз **с пустыми
   заданиями** — иначе его нельзя опубликовать.
8. Отметить практикум в `map.yaml`: `status: ready`, `notebook`, `kaggle`.
9. Опубликовать (см. ниже).

Шаги 2 и 3 автоматизируются частично, шаг 4 — нет: `trigger` и `traps` пишутся
руками, и это основная работа.

## Публикация на Kaggle

Каждый готовый практикум выкладывается на Kaggle приватным ноутбуком. Источник
правды — `map.yaml`: у практикума со `status: ready` должны быть поля `notebook`
и `kaggle`.

```bash
python practicum/make_kaggle.py C3 --push      # один практикум
python practicum/make_kaggle.py --all --push   # все готовые
python practicum/make_kaggle.py B4:archive     # архивный ноутбук темы
```

Скрипт встраивает `kit.py` в ячейку настройки, потому что на Kaggle соседних
файлов рядом нет, и сохраняет хвост этой ячейки — там объявляются символы,
специфичные для темы. Собранное кладётся в `build/kaggle/` и не версионируется.

Три вещи, о которых легко забыть.

**Kaggle исполняет ноутбук при заливке.** Если ячейка задания падает на
незаполненном `...`, публикация получает статус `ERROR`. Ради этого все проверки
в `kit.py` распознают placeholder и печатают `⬜`. Выражение, собираемое из
нескольких ответов, надо оборачивать в явный `if ... is not Ellipsis`.

**Заголовок должен совпадать со slug**, иначе Kaggle ругается на несоответствие
имени и адреса. Скрипт ставит их одинаковыми.

**Приватность по умолчанию.** В заданиях условия из past papers IB, репозиторий
приватный по той же причине. Флаг `--public` существует, но это осознанное решение.

Токен: у нового формата Kaggle (строка вида `KGA...`) это `~/.kaggle/access_token`
или переменная `KAGGLE_API_TOKEN`. Старый формат `username` + `key` в
`kaggle.json` свежий CLI не принимает и отвечает `401`, даже когда токен рабочий.

## PDF для чтения с телефона

Ноутбук в дороге не запустить, а читать и думать над ним хочется. Обычный A4
на телефоне заставляет зумить и таскать страницу вбок, поэтому страница здесь
делается узкой — по умолчанию **90×160 мм**, примерно пропорции экрана.

```bash
python practicum/make_pdf.py A4                       # один практикум
python practicum/make_pdf.py --all                    # все готовые
python practicum/make_pdf.py B4:archive               # архивный ноутбук темы
python practicum/make_pdf.py A4 --tasks               # без раздела решений
python practicum/make_pdf.py A4 --page 76x135 --font 12
```

Собранное кладётся в `practicum/pdf/` и **версионируется**, в отличие
от Kaggle-сборки. Причина простая: файл нужен в дороге, где ни Python,
ни Chromium под рукой нет, — его забирают из репозитория готовым.
Одиннадцать практикумов, 668 страниц, 5,8 МБ на все.

Пересобирать после правки ноутбука надо руками: `--all` перезаписывает
каталог, и в коммит попадают только те файлы, которые действительно
изменились.

Формулы верстает **KaTeX**: он скачивается один раз в `~/.cache/ib-practicum`
и дальше работает офлайн. Печатает headless Chromium через `playwright`
(`pip install playwright markdown-it-py mdit-py-plugins` и
`playwright install --with-deps chromium`). `pymupdf` необязателен и нужен
только для косметики закладок.

Титульная страница собирается из `map.yaml` и первой ячейки ноутбука: тема,
число заданий, есть ли решения, ссылка на интерактивную версию на Kaggle.
Навигация — закладки PDF по всем заголовкам, их читалка показывает
оглавлением.

### Что здесь оказалось неочевидным

Почти всё время ушло не на вёрстку, а на четыре ловушки. Все четыре
проявлялись одинаково: PDF выглядел правдоподобно и был неправильным.

**Chromium при печати ужимает весь документ по самому широкому элементу.**
Одна длинная выкладка, не влезающая в колонку, — и заказанные 10,5 pt молча
превращаются в 7 pt по всему файлу. Страниц становится *меньше*, поэтому
по объёму подвох не виден: первая версия занимала 38 страниц вместо 77
и была нечитаемой. Ловится сравнением кегля в готовом PDF с заказанным.

**Замеры идут в экранном вьюпорте, а не в ширине печатной страницы.**
По умолчанию это 1280 px, при которых ни одна формула широкой не выглядит,
и подгонка не срабатывает вовсе. Вьюпорт нужно сузить до колонки.

**Мерить надо после `document.fonts.ready`.** Шрифты KaTeX грузятся
асинхронно; до их появления формулы свёрстаны запасным шрифтом и заметно
уже. Замеры выходят оптимистичными, а потом формулы разъезжаются обратно.

**Ширину содержимого не показывает почти ничто.** Внутренний `.katex`
в display-режиме блочный и всегда шириной с колонку. `scrollWidth`
занижает вынос при `text-align: center`. Имена внутренних классов меняются
от версии к версии — в KaTeX 0.18 `.base` стал `.katex-base`, и проверка
по нему молча перестала находить переполнение. Рабочий способ: прижать
формулу влево на время замера и взять `scrollWidth`, а для строчных —
`Range.getBoundingClientRect()`.

### Длинные формулы режутся, а не ужимаются

Ужать выкладку до 52% кегля — значит сделать её нечитаемой ровно там, где
она важнее всего. Поэтому широкая формула сначала разбирается на части
по операторам **нулевого уровня вложенности** и собирается обратно
в `aligned`:

1. по разделителям `,` и `\qquad` — формула вида «A = B, C = D» это
   перечисление независимых равенств, и резать её по знакам равенства
   нельзя: левая часть одного уедет в строку к правой части предыдущего;
2. по знакам отношения — цепочка `A = B = C` становится лесенкой;
3. по `+` и `−` — если строка всё ещё длинна.

Знак операции уходит в начало переносимой строки, а не остаётся в конце
предыдущей, — как переносят формулы в книгах. Сжатие осталось на случай,
когда резать нечего: одна длинная дробь или произведение скобок.

Глубина вложенности считается по `{}`, `()` **и** `[]`. Без круглых скобок
`(α+β+γ)x²` разрезается посередине, и получается лесенка из `(α+`, `β+`,
`γ)x²` — ошибка, которая в исходном TeX не видна совсем.

### Два бага, которые проверка бы не поймала

**`re.match` вместо `re.search`.** Заголовки в ноутбуках идут после
горизонтальной черты, то есть не в начале ячейки. `re.match` якорится
в позицию 0, и флаг `re.M` ему не помогает. Из-за этого `--tasks` не резал
решения, а счётчик заданий на титуле показывал 16 вместо 17.

**Заголовок раздела решений записан по-разному.** В A4 это `# Решения`,
в остальных семи — `# 🔑 Решения`. Регулярка требовала слово сразу после
решётки, поэтому `--tasks` молча работал ровно в одном практикуме из восьми.

## Тренажёр повторения

Практикум учит приёму. Держать приём в рабочем состоянии он не может: ноутбук
проходят один раз, а через месяц от него остаётся ощущение «я это видел».
Для повторения сделан отдельный вход — вкладка **«Повторение»** на
math.archik.tech, рядом с атласом и практикумами. Код в `practicum/drill/`.

**Планируется приём, а не задача.** Приёмов сейчас 159 в девятнадцати карточках,
и повторение расписано по ним. Задача — расходный материал: в режиме счёта она
собирается заново по зерну, и запас не кончается. Планировать задачи значило бы
вести банк руками, и он бы протух на третьем практикуме.

**Три режима.** Узнавание — условие на экране, ответ кодом приёма, считать
нечего; 267 условий взяты прямо из тренажёров распознавания, которые уже стояли
в конце каждого ноутбука. Счёт — параметрическая задача на конкретный приём,
ответ вводится и проверяется. Вперемешку — и то, и другое, вразнобой по темам.

Перемешивание не украшение. Подряд по одной теме учить приятнее, но именно это
даёт ложное чувство усвоенного: внутри практикума C1 и так известно, что задача
про треугольник. На экзамене задача приходит без подписи.

**Сессия собирается заранее.** Начальный экран спрашивает режим, темы, длину
(10, 20, 40 или без конца), порядок и брать ли только просроченное — и лишь потом
«начать». Настройки запоминаются. Число на плитке темы — сколько в ней заданий
именно в выбранном режиме, поэтому в режиме счёта девять тем сразу видно
отключёнными. В конце показывается итог: счёт, время на задание и приёмы,
на которых споткнулся.

**После ответа — разбор из карточки.** Вердикт проверки, затем подписанные
части: `приём`, `узнаётся по` (это `trigger`), `ход` (это `chain`, по шагам)
и — только при ошибке — `где срезаются` (это `traps`) вместе с эталоном. Ход
показывается и при верном ответе: сойтись в ответе можно и длинной дорогой,
а на экзамене это стоит времени. Своего текста тренажёр не пишет, всё берётся
из карточек приёмов.

**Карта приёмов.** Девятнадцать строк по числу практикумов, в строке квадрат на
приём, слева направо по ступеням лестницы. В квадрате число — сила приёма, цвет
по числу: одна зелёная лестница, светлота падает с ростом силы. Слева от строки
счёт практикума — среднее по приёмам, взвешенное баллами архива, которые за
ними стоят: приём с сорока баллами тянет строку сильнее, чем приём с четырьмя.

Пунктирный квадрат с точкой вместо числа — приём, которого ни разу не
показывали, и в среднее по практикуму такие не идут вовсе. Иначе новая тема
выглядела бы забытой, хотя её не начинали: «не начинал» и «забыл» — разные
болезни, и на карте у них разный вид, а не разный оттенок одного.

По нажатию — карточка целиком: признак узнавания, ход по шагам, ловушки, пара
условий на узнавание с ответами, сгенерированная задача на счёт с ответом и
кнопкой «другое задание», все вопросы архива этого приёма со ссылками на
страницы билетов и своя статистика. Ничего специально для карты не пишется —
всё это уже лежит в банке, собранном из карточек приёмов.

**«Тренировать приём».** Кнопка в шапке карточки: десять задач подряд на один
этот приём, минуя начальный экран. Дырка на карте видна именно здесь — стоишь
перед карточкой приёма, который просел, — и путь «вернуться в настройки, снять
все темы кроме одной, всё равно получить всю тему» тут лишний. Отбор идёт по
приёму, а не по практикуму: `/next` принимает `skills=`, и он сужает набор
поверх тем, а не вместо них.

Тренировка идёт **счётом**, а не вперемешку: задача на счёт собирается заново
по зерну и десять подряд не повторятся, а условий на узнавание в банке два-три
на приём, и десяток подряд из них превратился бы в заучивание ответа. Длину
сессии из настроек кнопка тоже не берёт — там её выбирали для смеси; здесь
всегда десять. Обычная сессия ничего этого не замечает: без `skills=` запрос
прежний.

Шкала считана, а не подобрана на глаз: ступени заданы в oklch одним тоном,
светлота падает монотонно, все семь в гамуте, и надпись внутри квадрата держит
не меньше 4.5:1 — на шести светлых чернилами, на самой тёмной белым.

**Вечер.** Одна кнопка собирает лист настоящих вопросов архива на заказанное
время, лист открывается или печатается одним PDF, работа делается на бумаге и
присылается одним сканом, разбор приходит на всё сразу. Меряется вечер минутами,
а не числом задач: минуты — это то, чем располагают, а на экзамене примерно
минута на балл.

Трудное место здесь одно — чьи это страницы. Обложка просит подписать каждую
страницу номером задания в правом верхнем углу, дешёвый проход зрением читает
угол и раскладывает, а лента миниатюр показывает раскладку на подтверждение:
ошибка стоит одного нажатия, а не балла. Не прочиталось — страницы делятся по
порядку, и лента об этом честно говорит.

Отборов четыре: темы, бумага, «только просроченное» и запрет на вопросы из
шести последних вечеров. Темы — не украшение: их проходят по одной, и вопрос
на бумаге по неразобранному приёму теряет полчаса, поэтому по умолчанию
отмечены только начатые. Запреты снимаются по одному, когда иначе набор не
собрать: лучше повтор билета, чем вечер из двух задач.

Собранный набор — ещё не вечер. Видно, что выпало, и это можно пересобрать:
«назад к отбору» выбрасывает черновик, новая сборка тоже. Считается вечер
только со «старта» — до него он не идёт ни в «продолжить», ни в память о
недавних вопросах, и работы не принимает. Иначе брошенный набор занимал бы
собой и то, и другое.

Набор живёт в базе, а не в браузере. Это не мелочь: задания берут в семь вечера,
а работу присылают в десять и с другого устройства, и пока сессия лежала в
localStorage, такой вечер был невозможен. Задание, по которому страниц не
прислали, помечается пропущенным и силу приёма не роняет — это решение не
решать, а не промах.

**Что меряется.** Время до первого нажатия отдельно от времени до отправки.
Первое показывает, узнан ли приём; второе — насколько быстро идёт счёт. Это и
есть главная причина делать тренажёр страницей, а не ноутбуком: в клетке такого
не измерить, там между «прочитал» и «запустил» лежит правка кода.

**Что показывается следующим:**

    вес = доля баллов практикума × просадка × трудность

Доли баллов измерены и лежат в карточках: B1 весит 222 балла архива, C4 — 83,
и повторение это учитывает. Просадка — сколько от приёма утекло с последнего
раза. Приём, ни разу не показанный, идёт впереди всего остального: пробел хуже
подзабытого.

**Сила приёма вместо ящиков Лейтнера.** Ящики отвечали на один вопрос —
показывать сегодня или нет. Карта приёмов требует большего: насколько приём
отточен, а это две величины сразу. Стойкость `S` — через сколько дней приём
просядет до девяноста процентов; растёт от повторений, сама не падает.
Свежесть `R` — сколько от неё осталось сейчас. Число на карте — произведение
одного на другое, `100·R·ln(1+S)/ln(121)`, и сто означает приём, который
держится сто двадцать дней.

Считать по одной свежести нельзя: наутро после занятия приём, взятый вчера
впервые, и приём, отработанный за полгода, оба покажут сто. Считать по одной
стойкости тоже — она не знает, что три месяца никто ничего не повторял.

Прирост стойкости пропорционален `1 − R`: дорого стоит повторение, сделанное
тогда, когда приём уже поплыл. Эффект интервала берётся отсюда сам, и зубрёжку
не приходится запрещать — она просто не работает: пять повторений по лестнице
1-3-7-21 дают стойкость 27 дней, пять подряд — 6.8. Срок теперь и есть
стойкость, поэтому потолка в двадцать один день больше нет, и лестница уходит
за год — своей крутизной у каждого приёма.

Оценка попытки берётся из времени, которое до сих пор писалось в журнал и не
использовалось никем: вчетверо быстрее бюджета — `easy`, в бюджете — `good`,
дольше бюджета — `hard`. Верно-но-медленно перестало означать знание.

**Проверяет тот же `kit`.** Ответ не сравнивается строкой: написанное от руки
разбирается sympy (`2sqrt(6)`, `2√6`, `x^2`, `1, 4`, `нет`), а дальше работают
`check_num`, `verify_exact`, `verify_root_set`, `verify_triangle` — ровно те же,
что в ноутбуках. Поэтому «верно» здесь и «верно» там означают одно и то же,
и второго набора правил не заводится. Сообщения проверок показываются целиком:
«это десятичная запись, а вопрос просит точное значение» стоит прочесть.

**Эталон странице не отдаётся.** Задание на счёт восстанавливается по зерну,
когда приходит ответ, — хранить между запросами нечего, и подсмотреть в трафике
нечего тоже.

**Генератор задач опаснее, чем его отсутствие:** он способен уверенно и
бесконечно учить неверному. Поэтому `verify_drill.py` гоняет каждый генератор
по пятнадцати зёрнам и проверяет трижды — эталон проходит собственную проверку,
испорченный ответ ею отвергается, и ответ сходится с независимым выводом,
написанным заново от условия. На третьем пункте сразу нашёлся дефект: в задаче
на иррациональное уравнение при `√(x−3) = x−3` корней два, а генератор обещал
один.

**Сейчас закрыто.** Узнавание — все девятнадцать практикумов, 267 условий. Счёт —
**все 159 приёмов**, по модулю на практикум в `practicum/drill/items/`. Задача
собирается заново по зерну, поэтому запас не кончается.

Ответы разные по природе, и каждый уходит в свою проверку из `kit`: множество
решений неравенства сверяется с самим неравенством, корни на отрезке —
сканированием, решение дифференциального уравнения — подстановкой, комплексное
число — с точностью до формы записи, разложение на простейшие дроби — и по
равенству, и по форме. Ни одна из них не заведена заново: это ровно те функции,
что стоят в ноутбуках.

**Две проверки, добавленные вместе с B5.** `verify_model` берёт данные из
условия и подставляет их в ответ; эталона нет вовсе, и ошибка «постоянные
верны, отсчёт времени сдвинут» становится видимой — проверка называет ту
точку, где модель разошлась с условием. `verify_in_terms_of` сторожит ответ
«через p и q»: требует, чтобы чужих букв в нём не было и чтобы подстановка
истинных значений давала нужное число.

У первой по дороге нашёлся дефект, которого в ноутбуке было не заметить.
Сверка шла до трёх значащих цифр, а данные в условии точны: модель,
промахнувшаяся на 150 человек из 15 000, проходила как верная. Поймал это
не практикум, а `verify_drill.py` — он портит эталон на единицу и требует,
чтобы испорченный ответ был отвергнут, и `15000·e^{kt} + 1` проходил на всех
пятнадцати зёрнах. По умолчанию теперь шесть цифр; округлённое значение
подаётся с явным `sf`, и это уже не умолчание, а решение автора задания.

**Две проверки, добавленные вместе с E1.** `verify_limit` не знает ответа
вовсе: он берёт выражение из условия, подходит к точке лестницей
`10⁻¹ … 10⁻⁸` с обеих сторон и спрашивает, туда ли садятся значения. Это
десятое понятие равенства ответов в серии и самое радикальное — сверять
здесь не с чем. `verify_indeterminate` называет форму, глядя на числитель
и знаменатель порознь, и сторожит балл за лицензию применить правило.

У первой тоже нашёлся дефект, и опять его поймал не ноутбук. Первая версия
усредняла ошибку по верхней половине лестницы, и верный ответ `3/2` для
`(3x-1)/(2x+1)` на бесконечности отвергался: на ступени `10⁵` ошибка ещё
`10⁻⁵`. Считать надо по самой тонкой ступени.

**Чего тренажёр не проверяет — доказательство.** Напечатанным ответом его не
проверить, и притворяться незачем. В A7 и в приёме `prove_inequality`
спрашивается счётное ядро: база индукции, разность `S(k+1) − S(k)`, вынесенный
множитель `7^{k+1} − 4^{k+1} = 7(7^k − 4^k) + c·4^k`, остаток от деления
квадрата нечётного на 8. Это те места, где на экзамене и теряют баллы; само
рассуждение остаётся за практикумом.

**Режим разбора письменной работы.** Тренажёр проверяет ответ, а экзамен
платит за запись: M1 не ставят, если ход не виден, R1 — если вывод не написан
словами. Четвёртый режим закрывает это. Задание выдаёт тот же планировщик —
настоящий вопрос архива по приёму, которому подошёл срок, — страница билета
показывается картинкой из PDF. Решаешь на бумаге, по-английски, прикрепляешь
снимок. Оценок две и они не смешиваются — математика и оформление.

Вопросы отбираются не только по темам: можно взять конкретную бумагу
(Paper 1 без калькулятора, Paper 2 с ним, Paper 3 — исследования) и вилку по
цене (1–3, 4–6, 7 и больше). Двухбалльный вопрос и девятибалльный требуют
разного времени и разного нерва, и тренировать их полезно по отдельности.

Сессия здесь короче: одно задание занимает минуты, поэтому предлагается 1, 3
или 5 работ, а бюджет времени считается от цены вопроса — полторы минуты на
балл. В перемешку разбор не подмешивается: она устроена для коротких заданий
подряд.

Разбор опирается на подлинник: страницы билета и схемы оценивания рендерятся
из PDF и уходят в модель картинками. Какая именно страница — ищется по самому
вопросу: номера страниц в корпусе шумят на единицу, непостоянно даже внутри
одной бумаги, и подсказка годится только как ориентир. Поиск по строке
с номером вопроса исправил первую страницу у 158 вопросов из 332 и у 66 схем. Это важно принципиально — пересказ схемы
из корпуса использовать нельзя, он получен извлечением и в доказанном случае
неверен. Проверка на пустом листе показала, что заземление работает: модель
перечислила потерянные баллы словами конкретной схемы («the arctangent addition
result from part (b) is not applied»), а не общими словами про индукцию.

Правила разметки берутся из первоисточника: в начале каждой схемы напечатан
раздел «Instructions to Examiners» — определения M, A, R и AG и правила их
применения, — и он уходит в запрос текстом. Он объявлен старше рубрики: где
рубрика требует большего, чем IB, побеждают инструкции. Чтение этих страниц
сразу окупилось — оказалось, что мой пункт требовал повторять AG-строку,
а IB прямо пишет, что повторять её не нужно.

Сверка рубрики с этими страницами построчно дала четыре расхождения и
четыре пробела. Расхождения: пункты обещали потерю «AG-балла», которого нет
(AG — это пометка «ответ дан в условии», а не балл); требование округлять
только в самом конце строже, чем IB, — он разрешает брать 3 з.ц. в следующий
пункт; придирка к лишним выкладкам после верного ответа противоречила правилу
«further working is ignored»; а системная роль велела не наказывать за
зачёркивания, тогда как §10 говорит обратное — зачёркнутое не проверяется
вовсе. Пробелы: упрощение окончательного ответа, запись калькулятора в ответе,
правило «из двух ответов считается первый» и запрет альтернативного метода
после «Hence».

Рубрика оформления задана явно, 32 пункта в `presentation.yaml`, у каждого —
код теряемого балла и готовая английская фраза, которую следовало написать.
Строже всего A7: незаявленное предположение и отсутствие заключительной фразы
стоили в проверке M1, M1 и R1 при совершенно верной алгебре.

Всё сделанное сохраняется: снимки и сканы ложатся на диск **до** отправки
на проверку, поэтому работа не пропадает, даже если модель не ответила, —
в списке видно, что разбор не состоялся. Раздел «мои работы» показывает
список с датой, бумагой и оценкой, а внутри — страницы и весь разбор.

Оценка за оформление считается отдельно и в силу приёма не идёт: качество
записи — свой навык. Математическая часть идёт и весит больше всего остального
(1.4 против 1.0 у счёта и 0.6 у узнавания): это единственное свидетельство,
снятое в условиях экзамена. Оценку даёт доля набранных баллов, а не секундомер
— на бумаге он мерил бы скорость письма.

**Каждый новый практикум приносит свой банк.** Иначе тренажёр навсегда останется
на той трети материала, что была на день, когда его написали.

## Открытые вопросы

**Как проверять «покажите, что».** Вопросов вида *show that* в архиве много,
а ответ в них дан в условии, поэтому проверять нечего — важна цепочка. Сейчас
такие задания отправляют читать решение. Возможный ход — просить упорядочить
перемешанные шаги `method_path`.

В E2 вопрос перестал быть теоретическим: четыре из двадцати семи вопросов
архива — «show that», и это самые содержательные из них (ряд для sec x,
произведение двух биномиальных, ряд для arctan интегрированием). Ячейки
у них нет, и на этом теряется примерно 13 баллов из 99. Перемешанные шаги
здесь выглядят особенно уместно: у ряда цепочка короткая и её порядок
и есть решение — «узнать ряд», «подставить», «возвести в квадрат», «собрать
подобные», «вернуть множитель».

E3 вопрос сузил. Половина его вопросов — «show that f′(x) = …», и там
ячейка **осталась**: производную всё равно надо написать, и типизация её
сообщает, сошлась ли алгебра. Списать напечатанное можно, и ноутбук
говорит об этом прямо — он не сторожит, а отвечает. Без ячейки остались
только те четыре, у которых сдавать буквально нечего: ответ не выражение,
а сумма с многоточием или неравенство.

Отсюда правило, которого раньше не было: **напечатанный ответ отменяет
ячейку не сам по себе, а только когда он не выражение.** «Show that
f′(x) = 2/(2−x)³» проверяемо и стоит проверять; «show that
x f′(x) = x + 2x² + … + nxⁿ» — нет. Перемешанные шаги остаются нужны
именно для второго случая, и в E3 их четыре из пятидесяти пяти вместо
четырёх из двадцати семи.

**Генератор вариаций — сделан на всех собранных практикумах.** Параметрические
задачи с проверкой через sympy лежат в `practicum/drill/items/`, по модулю на
практикум, 159 генераторов на 159 приёмов. Следующий шаг не количественный:
в тренажёре нет ни одного настоящего вопроса из архива, а в ноутбуках их 300,
и у каждого уже есть работающая проверка.

**Заметки на полях классификации.** При сборке практикума хорошо видны ошибки
разметки. Стоит завести обратный поток в `classification/reviews`, иначе
наблюдения потеряются.

**Порядок прохождения.** Внутри раздела он очевиден, между разделами — нет.
Зависимости через `prereq` в карточках приёмов есть, но сквозного порядка
по всем 36 практикумам пока не выстроено.
